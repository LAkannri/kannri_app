# CLAUDE.md

このファイルは、このリポジトリで作業する Claude Code 向けのガイドです。

## ⚠️ 最重要ルール：Git のコミット／プッシュ名義

このリポジトリへの参加は **協力者（collaborator）** としての立場です。

- コミット・プッシュは必ず **Claude** 名義で行うこと。
- オーナー個人（`kura-nakakura`）のアカウント名義では **絶対にコミット・プッシュしない**。
- 現在の git 設定（厳守）：
  - `user.name`  = `Claude`
  - `user.email` = `noreply@anthropic.com`
- 作業ブランチは `claude/...`（例：`claude/dazzling-shannon-02iziu`）を使用し、
  許可なく `main` へ直接プッシュしない。
- プッシュ後は PR を作成（無ければドラフトで作成）する。
- モデル識別子（`claude-opus-...` 等）をコミットメッセージ・PR・コード・コメントに含めない。

## 🎯 このアプリの目的（エンカンAI）

事務作業の **キャリア申請（電気・ガス・ネット）を完全自動化** する RPA システム。

SFA（Google スプレッドシート）の案件データを元に、各キャリアの Web 申請フォームへ
Playwright で自動入力する。担当者が一度だけ「手本」を録画すれば、AI（Gemini）が
日本語の手順書を自動生成し、以後はロボットが代理入力する、という思想で作られている。

対象ユーザーは非エンジニアの録画担当者。そのため UI と文言は徹底的に「やさしく」
設計されている（絵文字・丸みフォント・かみ砕いた説明）。

## 🏗️ アーキテクチャ

| 役割 | 技術 |
|---|---|
| 顔（UI） | Streamlit（マルチページ） |
| 脳（DB） | Supabase（`merchants` テーブル）+ Google Spreadsheet（SFA） |
| 手足（自動操作） | Playwright（Chromium。ローカルは表示／クラウドは `headless`） |
| AI（手順生成） | Google Gemini（`gemini-2.5-flash`） |

### データモデル

ロボットの設定はすべて Supabase の `merchants` テーブルに JSON で保存される。

- `id` / `name`：ロボット名（= 主キー）
- `is_active`：稼働中かどうか
- `connector_type`：`"playwright"`
- `config_json`：
  - `product_type`：商材種別（ネット／電気／ガス／その他）
  - `spreadsheet`：SFA の `url` / `tab_name` / `trigger_col`(=ステータス) / `trigger_val`(=未エントリー)
  - `robot_config`：`target_url` / `steps`（手順書）/ `stealth` / `captcha`
  - `notifications`：`slack_id` / `slack_msg`
  - `conditions`：条件分岐ルール（パターン）一覧 ← 下記「ルールエンジン」参照

### 手順書（steps）の形

各ステップは日本語キー（旧）と英語キー（新）の両対応。`robot.py` が両方を吸収する。

```json
{ "順番": 1, "いつ": "常に", "操作": "文字を入力", "対象": "お名前",
  "値": "{顧客_氏名}", "変換": "", "ai_code": "..." }
```

- `操作` は `文字を入力→fill` / `クリック→click` / `選択→select` / `チェック→check` にマップ。
- `値` や `ai_code` 内の `{項目名}` は実行時に顧客データ（スプシ行）で動的置換される
  （例：`{電話番号}`）。`090` などが数値化しないよう純粋な文字列置換で行う。
- `変換`（任意）：置換後の値に加工を適用（コード不要）。`robot.py:apply_transform` が処理。
  対応：`ハイフン除去` / `数字のみ` / `市外局番` / `市内局番` / `加入者番号` /
  `郵便番号_上3桁` / `郵便番号_下4桁`。

### 録画では表せないもの（特別な操作）

録画は「そのとき押した場所」を覚えるため、**毎回変わるもの**は録画のままでは翌日使えない。
手順書の側で意図を書けるようにしてある。詳しい仕様と限界は `docs/機能説明書.md` の 5-1-2 に。

| 操作 | 書き方 | 中身 |
|---|---|---|
| `日付を入れる` | 値に `{日付の列}` | 文字で打つ→駄目ならカレンダーを操作（`_set_date_field`） |
| `ファイルをダウンロード` | 対象に `最新のファイル` | ファイル名の日時がいちばん新しいリンクを押す（`_newest_download_link`） |
| `ファイルをアップロード` | 値に `{アップロードファイル}` | 実行時に渡されたファイルを選ぶ（録画時のパスは捨てて差し替える） |
| `メールのリンクを開く` | 値に設定の名前 | メールで届く**使い切りのログインURL**を受け取って開く（`open_mail_link`） |
| `選択` | 値に `出てきた1つを選ぶ` | そのとき出ている選択肢が1つならそれを選ぶ。2つ以上なら止める（`_select_only_option`） |

`空のとき` 列で、セルが空のときの扱いを項目ごとに決められる
（`入力する`（既定）／`飛ばす`／`止める`）。

⚠️ この2つは「サイトごとに事情が違う」領域。新しいサイトで通じない形が出てきたら、
**一律に変えず、書き方（対象・値）で意図を足せるように拡張する**こと。
どのファイル・どの日付を選んだかは必ずログに出し、担当者が確かめられる状態を保つ。

### 分岐ルールエンジン（`conditions`）

ハードコードを廃し、設定駆動で評価する（`robot.py:evaluate_condition`）。
各ステップの `いつ` にルール名を指定すると、その条件成立時のみ実行される。

```json
{ "name": "未成年ルート", "logic": "AND",
  "rules": [ { "col": "年齢", "op": "lt", "value": "20" } ] }
```

- `op`：`eq`/`ne`/`contains`/`not_contains`/`empty`/`not_empty`/`gt`/`gte`/`lt`/`lte`/`in`
- `logic`：`AND`（全条件）/ `OR`（いずれか）。同名ルールに条件を追加すると複数条件になる。
- `常に`・空 → 必ず実行。**未定義のルール名 → 安全側でスキップ（False）**（事故防止）。
- 「条件で違う値を入れる」は、同じ `対象` の手順を複数行つくり、各行の `いつ` に
  別ルールを指定して実現する（値マッピング専用スキーマは設けていない）。

### 送信（申請）ステップ＝本番のみ実行（`SUBMIT_MARKERS`）

録画は**申請ボタンの“直前”まで**を手本にする思想のため、AI生成手順には最後の送信が含まれない。
そこで「最後の一押し」だけを別管理する。手順の `いつ` を `送信（本番のみ）`（`robot.py:SUBMIT_MARKERS`
＝`送信`/`申請`/`送信時` 等）にすると、その手順は**送信ステップ**として扱われる。

- `run_robot(..., allow_submit=False)`（お試し／モック単体実行）→ 送信ステップは**スキップ**（申請手前まで）。
- `run_all_active` の本番 LIVE（既定 `allow_submit=True`）→ 送信ステップを**実行**＝実際に申請まで完了。
- 送信ステップは条件評価をバイパスして必ず実行（直前のエラーは `has_critical_error` で停止するため安全）。
- 司令室の「🚀 送信ステップを追加」で、申請ボタンの文言を指定してワンクリック追加できる。
  送信ステップが無いロボットは本番でも申請が完了しないため、司令室で警告を表示する。

## 🤖 共通ロボット（`common_robots.py` ／ 登録は「その他設定」）

⭐ **このアプリで一番大事な考え方**：SFコネクタの更新も、GASのCSV書き出しも、プッシュプロの送信も、
**どのスプレッドシートでも押す場所は同じ**。違うのは「どのシートを選ぶか」「どのファイルを渡すか」だけ。
だから**録画は役割ごとに1台だけ**にして、実行時に値を差し替える。スプシが増えても録画し直さない。

| 役割 | 既定名 | 差し替わるもの |
|---|---|---|
| `refresh` | `共通_SFコネクタ更新` | **1周ごとに開くURL**（そのシートの `#gid=`）。手順そのものは変えない |
| `send` | `共通_プッシュプロ一括送信` | 値の `{アップロードファイル}` にその日のCSVのパスが入る |
| `autocall` | `共通_ブルービーン投入` | `{アップロードファイル}` と、**シートごとの `{タイトル}` など**（`--var`） |

- 登録画面は `pages/99_⚙️_その他設定.py` から `common_robots.render(supabase)` を呼ぶ。
  各ページは録画UIを持たず、**名前を選ぶだけ**。
- `robot.py --run` の追加オプション：`--var 名前=値`（1回だけ差し込む）／
  **`--each 名前=A,B,C`（ブラウザを開いたまま、値のぶんだけ同じ手順を繰り返す）**／
  **`--each-url "url1 url2 …"`（1周ごとにそのURLを開いてから手順をなぞる）**。
  更新のたびにブラウザを開き直すと遅く失敗も増えるので、1回開いて回す。
- ⚠️ **SFコネクタは「いま開いているシート」を更新する**（`Refresh Current Sheet Data`）。
  シート名を選ぶ画面は無いので、**手順ではなく開くURLでシートを切り替える**
  （`sms_runner.tab_urls_for`）。録画にシートタブのクリックは含めなくてよい。
- ⏳ **更新の終わりを待つステップ**（必ず入れる。無いと次のシートへ移った時点で打ち切る）
  | 操作 | いつ使う | 中身 |
  |---|---|---|
  | `出るまで待つ`（`wait_appear`） | 終わると合図が**出る**（SFコネクタは `The data has been refreshed.`） | `対象`の文字が出るまで待ち、**出た窓を閉じる** |
  | `終わるまで待つ`（`wait_done`） | 処理中だけ文字が**出ている** | 消えるまで待つ（15秒出なければ済んだとみなす） |
  | `待つ` | 文字では分からない | `値`の秒数だけ待つ |
  `値`＝最大何秒だが、**ふつうは空でよい**（`robot.WAIT_LIMIT_DEFAULT`＝1時間まで待つ）。
  かかる時間はレポート次第でまるで違うので、担当者に秒数を決めさせない。
  時間切れは**失敗として止める**（取りこぼしたまま進めない）。
  アプリ側の持ち時間（`_run_robot_cli` の timeout）も**シート枚数から自動で決める**
  （1枚あたり1時間ぶん確保。ロボットはまだ待っているのに先に打ち切る、を防ぐ）。
- ⚠️ **`page.get_by_role` は iframe の中まで探さない**。スプシの拡張機能（アドオン）の
  ボタンは小窓の中にあるので、録画の `ai_code` が空振りすると何も見つけられなかった。
  最後の砦として **`_find_anywhere`**（全フレームを巡回・`robot_config.find_wait_sec` 既定90秒まで
  待ちながら探す・`_text_variants` で読点/句点のズレと断片も試す）を入れてある。
  録画の書き起こしで `Refresh, Manual…` が `Refresh. Manual…` になる、といったズレを吸収する。
- ⚠️ **`_marker_on_page` は iframe の中も見る**。アドオンの窓は埋め込みの小窓なので、
  本体の文字だけ見ていると気づけない。
- ⏱ **`networkidle` を待ってはいけない**（`_open_sheet`）。スプシは開いている間ずっと
  通信するので、通信が止まる瞬間が来ない。以前はシートを開くたびに
  **タイムアウトいっぱい（20秒＋30秒）待ってから**動き出していた。
  「シートのタブ名が読めたら使える状態」で判定する（`_sheet_ready`）。
  何秒かかったかはログに出す（遅いと気づけるように）。
- ⚠️ **`#gid=` を変えるだけの移動はページを読み込み直さない**＝出ている窓が残る。
  `_close_dialog`（Esc → ✕/OK）で閉じてから次の周へ進む。周のはじめにも保険で1回閉じる。
- `run_robot(..., repeat_key=, repeat_values=)` が繰り返しの本体。1周でも失敗したらそこで止める。
### 🔐 ログインと二段階認証（鍵つきスプシ／プッシュプロ）

鍵つきのスプレッドシートは、ロボットが開くと **Googleのログイン画面**になる。毎回そこで人を待つのは
現実的でないので、**ロボットごとの専用Chromeプロファイル**（`robot.profile_path`）に
ログイン状態を貯める。二段階認証も、そこで一度「このデバイスを信頼する」まで済ませれば以後出ない。

ログインの扱いは役割ごとに分ける（`common_robots.ROLES`）：

| 旗 | 意味 |
|---|---|
| `has_login` | ログイン操作がある＝**IDも伏せる**（既定ON）／`目印`のボタンを出す／鍵の登録を出す |
| `login_url` | **先にログインしておく**（Cookieだけで済ませる）を使う |

- `refresh`：両方ON。二段階認証があるので、まず人が一度ログインしておく。
- `send`（プッシュプロ）：`has_login` のみ。**ログイン画面から録画し、手順書に組む**。
  パスワードは伏せ字→`{秘密:パスワード}`、IDも既定で伏せる→`{秘密:ログインID}`。
  実際の値は暗号化して保存し、ログイン手順には `目印` を付ける。
- `robot.py --login <ロボット名> [URL]`（`open_login_browser`）：そのロボットのプロファイルで
  ブラウザを開き、**人が閉じるまで待つ**だけ。閉じた時点で Cookie が残る。
  画面は「その他設定 → 🤖共通ロボットの登録 → 🔐 先にログインしておく」。
- **AIに何を送るか**（暗号化とは別の話）：録画コードは `steps_ai.redact_passwords()` を
  通したものだけを Gemini に渡す＝**パスワードは送られない**。ログインIDは既定では送られるが、
  `redact_logins()` と録画画面のチェックで伏せられる（ログインのあるロボットは既定ON）。
  伏せ字が0件だったときは画面で警告する（「伏せます」と言った以上、黙って通さない）。
- ⚠️ **ログインの手順は「1つずつ」では見分けられない**。IDは伏せずに素の値
  （例：`pa0002`）で入れることが多く、`{秘密:}` も『ログイン』の文字も持たないため、
  **ID欄だけ取りこぼして「欄が見つかりません」で止まっていた**（進捗反映で実際に起きた）。
  いまは `login_step_indexes` が、**ログインボタンの手前に並ぶ入力**と
  **その後ろに続く認証（コード入力・メールリンク）**までを1つのかたまりとして扱い、
  `_login_needed` が「ログイン画面ではない」と判断したら**まとめて飛ばす**
  ＝ **ログインボタンの次の操作から始める**。周（`--each`）ごとに1回だけ判断する。
- ⚠️ **『対象』の文字だけで「ログイン済み」と決めてはいけない**。対象は録画のときに付けた
  呼び名（例：`ログインID入力欄`）で、画面に同じ文字があるとは限らない。文字が無い＝ログイン済み、
  と決めつけたせいで、**ログイン画面が出ているのにIDもパスワードも入れずに素通りした**
  （ドコモGMOの進捗反映で実際に起きた）。いまは **`input[type=password]` が見えているか**を
  先に見る（`_password_box_visible`）。パスワード欄はログイン画面にしか出ず、サイトの言い回しに左右されない。
  どちらの判断をしたかは必ずログに出す（飛ばした／ログインした、を後から確かめられるように）。
- ⚠️ **開いた直後に1回見るだけでは早すぎる**。あとから画面が組み上がるサイトでは、
  ログイン画面が出る前に「ログイン済み」と決めつけてしまう。`login_check_sec`（既定15秒）まで
  待ちながら、①パスワード欄 →②「ログアウト」の文字 →③手順の対象、の順に見る
  （③は1つ5秒かかるので最後）。判断は**実行ごとに1回**。周ごとに調べ直すと、シートの数だけ待ち時間が増える。
- ⭐ 飛ばしたあとで「欄が見つかりません」になったとき、パスワード欄が出ていたら
  **『ログイン済みの判断が誤りでした』と名指しする**（原因の取り違えを防ぐ）。
- **ログインを手順書に組む場合**：ID・パスワードは司令室と同じ `{秘密:名前}` で暗号化保存
  （`robot_settings_ui.render_login_secrets` を共通ロボットの画面からも呼んでいる）。
  ⚠️ ただし**ログイン済みの日は入力欄が出ない**ので、そのままだと「欄が見つかりません」で止まる。
  そこで各手順に **`目印` 列**を用意した（`robot.py`：その文字が画面に無ければその手順を飛ばす）。
  ログイン手順の目印に `パスワード` などを入れておけば、**ログイン画面が出た日だけ入力**する。
  登録画面に「🎯 ログインの手順に目印を付ける」ボタンあり（目印が空のログイン手順を検出して警告）。
- 保険として、手順の先頭に **`人の操作を待つ`（対象＝ログイン／値＝パスワード）** を足せる
  （登録画面のボタン）。`値`＝**目印**なので、ログイン画面が出なかった日は素通りし、
  出た日は人がログインし終えた（目印が消えた）ら自動で先へ進む。
- ⚠️ headless（CI）では人が操作できないため、`人の操作を待つ` は中止になる。
  SFコネクタの更新・SMS送信はもともと担当者のPCで動かす前提。

- ⚠️ **録画のときに入れたURLは「録画専用」**。実行時は必ず、そのパターン／ジョブに登録した
  スプシURLで `--url` 上書きして開く（＝どのスプシで録画しても、全スプシで使える）。
  ここは誤解されやすいので、実行画面に「開くスプレッドシート：…」を必ず表示すること。
  1枚だけ試せる「🧪 この1枚だけ試す」も用意してある（別スプシで通るか確かめるため）。
- **`対象` にも `{項目名}` が使える**（シート名をクリックで選ぶ作りに対応するため）。

## 🔄 SFレポート更新（エントリー業務自動化のホームから）

`report_refresh.py`（`pages/2_...` のホームにある「🔄 レポートを更新する」から開く）。
エントリーを始める前に、**もとになるSFのレポートを最新にする**ための画面。
設定は Supabase の予約行 `__reports__` の `config_json.sets`。

⭐ **SMS送信・データローダーとの違いは「スプシをまたげる」こと**。あちらは
パターン／ジョブに紐づいた**1枚のスプシ**の中のシートを更新する。エントリー前に更新したい
レポートは**何枚ものスプシに散らばっている**ので、「更新セット」＝スプシ何枚ぶんかの
まとまり、として登録する。

- 1セット＝`{"name", "memo", "robot", "sheets":[{"label", "url", "tabs":[…]}]}`。
- 使うロボットは**共通1台**（`共通_SFコネクタ更新`）。更新の押す場所はどこでも同じで、
  違うのは開く先だけなので、録画はし直さない。
- 実行は `sms_runner.run_sheet_refresh(..., tabs=, tab_urls=)` を**1回だけ**呼ぶ。
  `--each-url` は1周ごとに別のURLを開けるので、**スプシをまたいでも
  ブラウザは1回開くだけ**で回せる（ログインも画面移動も1回で済む）。
- 「🔄 ぜんぶ更新する」と「✅ チェックしたものだけ更新する」の2通り。
  一覧は `st.data_editor` のチェック欄なので、**1枚だけ／数枚だけ**も選べる。
- 結果は `sms_runner.refresh_results(log, 件数)` で**周の番号**から作る。
  ⚠️ `parse_refresh_log` はシート名で見るので、**別々のスプシに同じ名前のシート**が
  あると取り違える。スプシをまたぐこの画面では番号で見ること。
- ⚠️ **入力欄のキーは行番号ではなく行ごとの目印（`_uid`）に結びつける**。番号で付けると、
  途中の行を消したときに下の行が番号を引き継ぎ、Streamlit が覚えている前の値がそのまま出て、
  **消した行と違う行が消えたように見える**（実際に起きた）。
- ⚠️ **スプシへの問い合わせには待ち時間の上限を付ける**（`SHEET_TIMEOUT_SEC`＝30秒。
  `gspread` の既定は無制限）。Google側が詰まった1回のせいで、
  **設定画面が固まったまま**になった（実際に起きた）。

## 🗃 エントリー後の投入（エントリー業務自動化のホームから）

`entry_loader.py`（ホームの「🗃 投入する」から開く）。エントリーが終わった案件に
**「エントリー済み」を書き戻す**ための投入。設定は Supabase の予約行 `__entry_loads__` の
`config_json.sets`。

⭐ **「🗃 データローダー自動化」ページとの違い**：あちらは「このスプシを更新して、この投入をする」
という**ジョブ**の単位で、スプシは**1枚**に紐づく。こちらは**エントリー業務の後始末**なので、
**何枚ものスプシにまたがった投入**をひとまとめ（＝投入セット）にして、ボタン1つで通す。
画面の形はレポート更新（`report_refresh.py`）にそろえてある（一覧／設定／実行）。

- 1セット＝`{"name","memo","loads":[{"label","url","シート","オブジェクト","照合キー","マッピング"}]}`。
- ⚠️ **投入の編集と実行は、既存の共通部品を使う**：編集は `sf_ui.load_editor`、
  実行は `sf_ui.push_sheet`（SMS送信・データローダーと同じもの）。
  別に書くと、片方だけ直して食い違う。
- 実行画面はチェック欄つきの一覧＋「🩺 シートと照らし合わせる（送りません）」／
  「🧪 お試し（先頭N件）」／「🚀 全件投入」。
  ⚠️ **全件投入は取り消せない**ので、確認のチェックを入れないと押せない。
- 送る前の2つの確認（シートに列があるか／Salesforceに項目があるか）は
  `push_sheet` の中でも必ず行われ、欠けていたら**送らずに止まる**。

## 📱 SMS送信（プッシュプロ一括送信）

`pages/6_📱_SMS送信.py` ＋ `sms_runner.py`。**パターン**ごとに ①更新 → ①-2 作り直し →
②チェック → ③CSV用意 → ④一括送信。設定は Supabase の予約行 `__sms__` の `config_json.patterns`。

### ① 更新
`refresh_tabs`（複数）を `run_refresh_robot(..., tabs=...)` に渡し、**1回のブラウザで**回す。
どこまで進んだかは `parse_refresh_log` がログの `🔁 i/n：更新するシート = X` から表にする。

### ①-2 GASで「作成」を走らせる（`gas_build` がある設定だけ）

⭐ **作成は、CSVを作るときではなく②の確認より前に走らせる**（`_run_gas_build`）。
作成が走る前のシートを人が見ても、映るのは**前回の中身**で、確認したことにならない。
- 走ったことを `st.session_state["sms_gasb_<パターン>"]` に残し、③の `fetch_from_gas` には
  `build=""` を渡す。⚠️ **③で走らせ直すと、②で人が直したセルを作り直しで消してしまう**。
- 同じ理由で、確認の小窓でOKを押した「続き」（`resume`）でも走らせ直さない。
- まだ一度も走っていないときだけ、③が従来どおり `build` を渡す（設定を変えずに済む逃げ道）。
- 個別実行の画面は `1️⃣-2 GASでシートを作り直す`。押すと読み直し＋人のOKを取り消す。

### ② 中身の確認（人が見る／ルールは任意）

⭐ **一覧の「▶ 全部実行」は、押した瞬間から最後まで通す**（「🔧 個別実行」は工程ごとに押す）。
人の確認で止まったら、**その場で小窓（`st.dialog`）にシートを出し、直して・OKで続きへ**。
画面を探しに行かせないため。⚠️ **送信（SMS）と投入（Salesforce）は取り消せない**ので、
確認のチェックが入っていなければ、その手前（`⏸`）で必ず止める。
⭐ 毎回チェックし直すのが手間なら、**設定で「どこまで自動で行くか」を決めておける**
（SMS：`auto_send` / `auto_load`、データローダー：`auto_push`。既定はすべてOFF）。
ONにすると、その工程の確認は毎回のチェック無しで通る。実行画面には「⚙️ この設定では〜」と出す
（黙って送らない）。**送る前のチェック・二重送信の除外・エラー件数の確認・目視確認は、ONでも働く。**
データローダーも同じ（③まで通して、④の投入だけ確認待ちにする）。

⭐ **本体は「シート名だけ登録して、人が見てOKを出す」**（`patterns[].check_tabs`）。
実行の②でそのシートの中身をそのまま出し、**チェックを入れるまで③へ進めない**。
**登録が無ければ、確認の画面を出さずに素通りする。**
ルール化できないものを機械に判断させないための工程（データローダーの `watch_tabs` と同じ考え）。
「▶ ぜんぶ実行する」も、確認待ちのときは `⏸` で止まる。

### ②-2 ルールでの自動チェック（`check_rules`・任意）

⭐ **チェックの画面で、シートをそのまま出して、その場で直せる**（`sms_runner.write_cells`）。
ルールを組んでいなくても中身は見られる（組むのが面倒で使えていなかったため）。
「💾 書き戻す」→「✅ 直し終わった」でもう一度チェックが走り、0件になれば次へ進める。
- ⚠️ **書き戻しは `RAW` で行う**。`USER_ENTERED` はスプシに解釈させるので、
  携帯番号 `090…` を数値と見なして**先頭の0を落とす**（実際に落ちた）。
- ⚠️ **変えたセルだけ**を送る（表ごと上書きすると、画面に出していない列や数式が消える）。
- 書き戻しには、そのスプシをサービスアカウントに**編集者**として共有しておく必要がある。

`空はNG` / `数字だけ` / `電話番号の形` / `重複はNG` / `この文字を含む` / `この文字を含まない` /
`決まった値のどれか` / `文字数の上限`。値は NFKC 正規化。`重複はNG` は数字だけの並びならハイフンを外して比較。
**0件になるまで③④を出さない**。

⭐ **CSVにするシートは複数持てる**（`patterns[].gas_sheets`／旧 `gas_sheet` も読む）。
**1シート＝1回の送信**で、③④をシートのぶん繰り返す。
CSVの置き場所はシートごとに分ける（`sms_runner.sheet_slot`＝`<パターン>／<シート>`。
同じ名前に上書きすると先に作ったほうが消えるため）。
⚠️ **「すでに送った宛先」の記録は、シートではなくパターンでまとめて持つ**
（`drop_already_sent(..., sent_pattern=パターン名)`）。
シートごとに分けると、**同じ人に両方の文面が届く**。

### ③ CSVの用意（`csv_source` 3通り／既定は「GASの成果物を使う」）

⚠️ 電話番号の頭の0付け・Shift_JIS化は各スプシの GAS が持っている。同じ整形をアプリに書くと
片方だけ直して食い違うので、**作り直さず受け取る**。

| 値 | 中身 |
|---|---|
| `GASのURLを叩いて受け取る（推奨）` | `fetch_from_gas`。GAS を**ウェブアプリとしてデプロイ**しておき、`?token=…&action=csv&sheet=…` を叩いて base64 で受け取る。録画も Drive の共有設定も要らず、「いま作られたもの」が確実に返る |
| `GASがDriveに書き出したものを使う` | `fetch_from_drive`。`DRIVE_SMS_ROOT` 下 `yyyy/M月/d/` から `drive_label` で始まる最新を取る |
| `アプリがシートから作る` | `export_csv`（GASの整形を通らないので警告を出す） |

🗑 **「ロボットにGASのボタンを押させて受け取る」（`export` の共通ロボット）は廃止した。**
GASのURLを叩けば同じCSVが返るのに、サイドバー（iframe）の録画は不安定で、結局だれも録画しないまま「まだ録画していない共通ロボットがあります」と出し続けていた。
**使わない道は残さない**（選択肢に並ぶだけで迷わせるため）。廃止した選択肢を保存しているパターンは、推奨のやり方として扱う。

⚠️ **合言葉を埋めるのは「宣言の1行だけ」**（`sms_runner.gas_template` の正規表現）。
以前は `text.replace()` で全部を置き換えていたため、`doGet` の中の
「まだ書き替えていないか」を見る行まで合言葉に変わり、**正しい合言葉を入れるほど必ず
『API_TOKEN が未設定です』になる**という、追いようのない不具合を出した。
GAS側も、判定用の文字は `['ここに','長い','合言葉を','書く'].join('')` と分けて持ち、
埋め込みの対象にならないようにしてある。**この2つはセットで守ること。**
`gas_template` は埋め込みに失敗したら例外を出す（壊れたコードを黙って配らない）。

**GAS側のコードは、アプリの設定画面が出す**（`sms_runner.gas_template`）。`gas/*.gs` を読み、
`'ここに長い合言葉を書く'` を**その場の合言葉に置き換えて**表示・ダウンロードさせる。
人がコードを探したり、合言葉を書き替えたりしなくてよい。
⚠️ **合言葉の行だけでなく `doGet` ごと貼る**必要がある。無いとHTMLのエラーページが返り、
以前は「ログインを求められています」と誤って案内していた（`intake_runner.call_gas` で見分ける）。
⚠️ URLが `/a/macros/<ドメイン>/…` の形だと、公開範囲を「全員」にしてもログインを求められる。
`sms_runner.gas_url_fixed`（`normalize_gas_url`）で直してから呼ぶ。

⭐ **貼るコードは全スプシ・全用途で同一**（`gas/エンカンAI_連携WebAPI.gs` 1本。SMSもデータローダーも）。スプシごとに違うもの（作成の関数名・シート名）は
**GASに書かせず、アプリ側に持たせる**（`gas_build` / `gas_sheet` をURLパラメータで渡す）。
人がコードを読んで書き分ける必要をなくすため。書き替えるのは合言葉の1行だけ。
- `action=inspect`：そのスプシの**シート名と関数名を返す** → アプリがプルダウンに出す
- `action=csv&sheet=…&build=関数名,…`：作成を走らせてからCSVを返す（SMS送信）
- `action=build&build=関数名,…`：処理を走らせ、全シートの行数を返す（データローダー）
- ⚠️ 書き出し先の設定はスプシごとに書き方が違う（`EXPORT_CONFIG` / `ROOT_FOLDER_IDS`＋
  `FILE_SUFFIX_MAP`）。`smsConf_` がどちらでも拾い、無ければシート名を使う。
- ⚠️ **確認の前に「作成」が要る**（`extractLifelineContacts_FINAL` などが `連絡先抽出` を作る）。
  SMS送信では ①-2 で走らせる（③では走らせない。上の「①-2」参照）。
  **その関数が `ui.alert` を使っていると人のいない実行では落ちる**ので、
  `smsRunBuilds_` が見分けて、直し方（`getUi` を try/catch に）を返す。

**GAS側**：`gas/SMS_CSV書き出しWebAPI.gs` を各スプシのスクリプト末尾に貼り、`API_TOKEN` を設定して
ウェブアプリ（実行：自分／アクセス：全員）としてデプロイする。CSVを作るのは既存の `buildCsvString_` のまま
（整形をアプリ側に写さない）。⚠️ **「全員」＝URLを知っていれば誰でも叩ける**ので、合言葉は必須。
デプロイURLはスプシごとに違うため、**パターンの設定に持たせる**（`gas_url` / `gas_token` / `gas_sheet`）。

⭐ **置き場所は `取り込みファイル/SMS送信用/<パターン名>/送信データ.csv` で固定**（`CSV_NAME`）。
毎回この名前で上書きするので、**録画で選んだファイルのまま動く**＝録画し直し不要。
日付つきの控えは `履歴/` に残す（`KEEP_HISTORY_DAYS`）。
`today_csv()` は**更新日時が今日でなければ None** を返す（前日の分を送らないため）。

### ④ 二重送信の防止と、送信の記録

プッシュプロは一括送信なので、**送った分は取り消せない**。だから3段構え：

1. **送る前に弾く**（②のチェック）
2. **送った宛先を記録**：`送信履歴.json`（`load_sent` / `record_sent`）。
   キーは **CSVの1列目**（GASのCSVはどれも1列目が携帯番号）を NFKC＋ハイフン除去で正規化（`_dest_key`）。
3. **記録から消せる**（`forget_sent`・SMS送信の「📮 送信の記録」）：プッシュプロ側で
   実際には送られていなかったと**人が確かめたときだけ**、宛先を消して再送できるようにする。
   歯止めを外す操作なので、確認のチェックを入れないと押せない。
   ⚠️ `dedup_days` は **暦の日で数える**（`still_blocked`）。時間で数えると、
   昨日の夕方に送った相手へ今朝は送れない（15時間しか経っていない）ため、
   担当者の言う「翌日なら送ってよい」と食い違う。
   `0`＝二度と送らない／`1`＝今日送った分だけ止める（＝翌日は送れる）。
4. **次のCSVから外す**：`find_already_sent`（判定は `still_blocked` に一本化）→
   `drop_already_sent` で該当行を落として同じ名前で置き直す。

記録する結果は3種類。**迷ったら「送った」に寄せる**（二重送信のほうが取り返しがつかないため）：

| 結果 | いつ |
|---|---|
| `送信済み` | ロボットが最後まで通った |
| `送信操作まで進みました` | 途中で止まったが、`submit_reached(log)` ＝送信ステップまで進んでいた |

⚠️ 真ん中は昔「要確認（送ったかもしれない）」と書いていたが、**読む人を不安にさせるだけで、
次に何をすればよいか分からない**という指摘を受けて改めた。**起きた事実だけ**を書き、
画面には「プッシュプロの送信履歴を見る → 入っていればOK／無ければ記録を消して再送」
という手順を出す。過去の記録も読めるよう `sms_runner.is_maybe()` で両方を拾う。
| `送信できず` | 送信の手前で止まった（記録を増やさない＝そのまま再送できる） |

- ⭐ **送る前にエラー件数を確かめる**（`数を確かめる`＝`robot.py` の `check_count`）：
  プッシュプロはCSVを取り込んだ時点で「エラーレコード件数 N 件」を出す。
  **送信ステップの直前**にこれを入れ、0件でなければ送らずに止める。
  読み取れなかった場合も止める（確かめられないまま送らない）。
  → 「一部だけ先に送られてしまう」を、**送る前に**防げる唯一の場所。
  ⭐ **止めたときは、理由も一緒に読む**（`_count_details`）。プッシュプロは
  『2行目｜1列目｜間違った携帯番号が入力されています。｜090192465022』のように
  表で理由を出すので、それをログに出す。件数だけでは何を直せばよいか分からない。
  ⚠️ 画面には件数の表もあるので、**エラーらしい言葉を含む表**を選ぶ（先頭の表を取ると外す）。
  画面のスクショも実行画面にそのまま出す（`sms_runner.shot_paths`）。
- ⚠️ **送信ボタンの文言に件数が入る**（`送信対象(1件)のSMSを送信する`）。件数は日々変わるので、
  `対象` から件数を外して `SMSを送信する` だけにし、`ai_code` は空にする
  （`robot.py` の click フォールバックは `exact=False` なので部分一致で押せる）。
  登録画面に「🔧 押す場所の文言から件数を外す」ボタンあり。
- 🧪 **送信の手前まで試せる**（その他設定の登録画面 → 手順を見る／直す → お試し実行）：
  CSVを渡して `run_send_robot(..., submit=False)` で動かす。`送信（本番のみ）`は飛ばすので
  **実際には送られない**。録画の手直しは何度も要るので、そのたびに本当に送るわけにいかない。
  渡すファイルは実行時に `--file` で差し込む（設定に持たせない＝録画し直し不要）。
- 🛑 **お試し実行は、印の無い「送信らしい手順」があると `robot.py` が起動前に中止する**
  （`unmarked_submit_steps`）。⚠️ 実際に事故った：送信の手順が2つあり、印つきの方は飛ばされ、
  『常に』の方が押されて**送信された**のに、ログは印つきを見て
  「スキップしました」と出た＝**送ったのに送っていないと表示された**。
  画面の警告だけでは、読み飛ばせば送れてしまう。**判断はロボット側に持たせる。**
  登録画面でも、送信らしい手順が既にあれば「🚀 送信ステップを足す」は**押せない**
  （足すと2つになるため）。2つあるときは「🧹 1つだけ残す」で直せる。
- 🚨 **送信の手順が『常に』のままだと、お試しでも本当に送る**。登録画面で名指しして
  ワンクリックで `送信（本番のみ）` に直せる。お試し実行は、その手順が無いと**押せない**
  （「送りません」と言った以上、確かめずに走らせない）。
- ⚠️ **`数を確かめる` は送信より前**でないと意味がない。後ろにあれば警告して移せる。
  送信ステップが無いうちは「🛡 エラーが0件のときだけ送る」を**押せない**
  （末尾に付いて「送ってから数える」並びになるため）。
- **偽の成功を防ぐ**：送信ロボットの `robot_config.success_text`（その他設定の登録画面で入力）を入れると、
  送信後にその文言を確かめ、出なければ失敗あつかいになる。空だと自動確認できない旨を画面で警告する。
- ⭐ **SMS非対応の番号は「直しようがない」**。1件のために全員に送れないと業務が回らないので、
  `patterns[].allow_errors`（既定OFF）で**送れる分だけ送る**を選べる
  （プッシュプロの「送信対象のSMSを送信する」と同じ。`robot.py --allow-errors`）。
  ⚠️ **弾かれた宛先は送られていないので、「送信済み」に入れない**
  （`_dropped_dests` → `dropped_dests` で拾って除外）。入れると、直したあとに送り直せなくなる。
  実行画面に「送られなかった宛先」を表で出す。
  ⚠️ **送れなかったお客様も、投入は行う**（外すと、その案件が翌日以降もずっと出てくる）。
  代わりに、**案件と「顧客対応備考」を実行画面に出す**（`sf_ui.find_case_ids` →
  `lookup_remarks`。既定の項目は `FormanagementRemarks__c`。**読むだけ**で書き替えない）。
  案件IDは、番号と同じ行にあるので**行の中を探して**引く（シート名も列名も設定させない）。
  **SF更新シート（投入シート）から先に見る**（`prefer_tabs`）。
  ⚠️ `連絡先抽出` のようなシートが無いパターンもあるので、1枚に決め打ちしないこと。
  ⭐ その場で**備考に書き足せる**（`sf_ui.append_remark`）。
  ⚠️ **上書きではなく、うしろに足す**（前に書いてあったことを消さない）。
  同じ文言が既にあれば書かない（毎日実行しても増えない）。
  ⚠️ `FormanagementRemarks__c` は長文テキストなので、**SOQLの検索条件に使えない**。
  Idで引いて中身を読むこと。
- ⚠️ **部分失敗は取り消せない**：一括送信なので、一部の番号が弾かれても残りは送信済みになる。
  アプリ側でできるのは「事前に弾く」「送った分を記録して再送から外す」まで。
  プッシュプロ側の送信結果は必ず人が確認する（結果CSVの取り込みは未実装・ロードマップ）。

## 🚀 進捗反映（画面の分け方と、確認用シート）

- ⚙️ **設定は別画面にしまう**（`st.session_state.pg_view`＝`main`/`settings`）。
  毎日使うのは「▶ 実行」と「確認」だけ。キャリアの登録・取り込み方は右上の**⚙️ 設定**から。
- 🔢 **実行の順番は、実行画面の「どのキャリアを実行する？」で決める**（`_save_pick_order`）。
  ⭐ 「いくつかのキャリアを反映してから、それを**集めた1枚のシート**を投入する」
  （ニチガスの反映 → ニチガスのデータローダー）という前後関係があるため。
  **選んだ順がそのまま実行の順**（`st.multiselect` は選んだ順で返る）。並べ替えは
  「ぜんぶ外す → やりたい順に選び直す」。「💾 この順番で保存する」で `順番` 列に書き戻す。
  ⚠️ **保存できるのは全キャリアを選び直したときだけ**。一部だけ選んだ状態で保存できると、
  「今日1社だけやり直す」つもりの選択が、明日からの並びとして残る（外した分が後ろへ飛ぶ）。
  ⚠️ **設定画面に順番の部品を置かない**。2か所で決められると、どちらで直したか分からなくなる。
  カードの「実行は N 番目」は**同じ貼り付け先スプシの中での順**（実行画面がスプシごとに
  分かれているため。通し番号で出すと食い違う）。
  ⚠️ 投入（`_targets`）も**同じ並び**にする。以前は `_done + _no_intake` だったため、
  `取り込み不要` のキャリアが**必ず最後**になり、順番を作れなかった。
  番号が空の行は「いちばん後ろ」（`_order_num`＝9999）＋元の並びのまま（`_sort_by_order` は
  stable）なので、番号を付けていないうちは、これまでとまったく同じ順で動く。
- ☁️ **1キャリアに投入は何本でも**（`sf_ui.carrier_loads` / `push_carrier_load`）。
  1本目は取り込み設定の1行（`投入用シート名`＋`オブジェクトAPI名`＋`外部IDキー`＋設定スプシのマッピング）のまま。
  2本目からは `__progress__` の `carrier_loads`（キャリア名 → 投入の並び）に持ち、**上から順に**投入する。
  ⚠️ 設定スプシに列を増やすとGAS（進捗メール添付の取り込み.gs）の並びに響くので、追加ぶんはSupabase側に置く。
  編集は `sf_ui.render_carrier_extra_loads`（中身は `load_editor`＝データローダー・SMSと同じ部品）。
  🗂 **設定画面は「📥 取り込み（2〜4）」「☁️ Salesforceへの投入（5）」のタブに分け、投入は枠ごとに出す**（担当者の指摘 2026-09-21：項目が多すぎて、どれが進捗の反映でどれが不備の反映か分からない）。
  5. は上から「🔀 見分け方（全部の投入に共通・`match_summary`）」「1️⃣」「2️⃣…」の枠。見出しの下に要点1行（`sf_ui.load_summary`）、細かい設定は「⚙️ 設定」の中。
  投入の呼び名：1本目は `__progress__` の `carrier_first_name`（`FIRST_NAME_KEY`・空なら「進捗の反映」）、2本目からは各投入の `名前`。**見分けるためだけ**で動きは変えない。
  ⭐ **1本目も `load_editor`（2本目・データローダーと同じ部品）で設定する**（`allow_empty=True`＝「（投入しない）」を選べる）。
  シート・投入先・照合キーはこれまでどおり取り込み設定の行（GASと同じ表）、マッピングなどは `__progress__` の `carrier_first_load`
  （`sf_ui.FIRST_LOAD_KEY`・`first_load_extras` / `save_first_load`。前の `carrier_no_overwrite`・`carrier_first_name` も読む）。
  ⚠️ **マッピングがまだ無いキャリアは、これまでどおり設定スプシのマッピング（`push_carrier`）で送る**。画面を開くと設定スプシのマッピングから始まり、保存した時点で移る。
  2026-09-21に13キャリアで、`push_carrier` と `push_sheet` の送る中身が同じことを照合済み（読むだけ）。
  実行は画面（`pages/3_…`）と時間指定（`auto_jobs.run_progress`）の**両方が同じ関数**を通る。
- 📞 **ID欄に電話番号・案件番号が入っている行は、送る直前に案件IDへ差し替える**（`sf_ui._swap_phone_ids` → `salesforce_loader.resolve_phone_ids`）。
  キャリアの進捗で案件IDが取れなかった案件は、分かる番号で埋めてあり、毎日同じ行が `Id in upsert is not valid` になっていた（INE SB＝電話番号／HTB＝案件番号。キャリア側は直せない）。
  電話番号 → 案件の「登録用」（`Opportunity.ForRegistration2__c`・ほぼ `090-1234-5678` の形。取引先側の「登録用」はほぼ空）。
  案件番号（英字2文字＋数字8桁） → 案件の「案件番号」（`ProposalNumber__c`・自動採番）。
  **ちょうど1件のときだけ**差し替え、0件・2件以上は送らずに「❓ 見つからなかった」で名指しする（電話番号は下4桁だけ）。スプシは書き換えない。
  進捗反映の投入（`push_carrier` と、`push_carrier_load` → `push_sheet(phone_ids=True)`）だけ。照合キーが `Id` の Opportunity のときだけ動く。
- 🔀 **その案件のいまのキャリアの進捗だけ上書きする**（`salesforce_loader.split_by_carrier` / `carrier_matches`・設定は `__progress__` の `carrier_match`＝キャリア名 → {項目API名: [値…]}）。
  ⚠️ 同じ案件を別のキャリアで取り直す（ドコモ光 → SB光、INE → LINES）と、前のキャリアの進捗に古い行が残って届き、
  今のキャリアで決まった工事日を古い値で上書きしてしまう（担当者の相談 2026-09-21）。かといって「違う値は上書きしない」だけだと、
  今のキャリアの正しい更新（工事日の変更）まで止まる。
  送る前に案件の `Product__c`（*商品）・`Field1__c`（ネットエントリー先）・`PowerCarrier__c`・`GasCarrier__c` を読み、
  **合えば違う値でも上書き／合わなければ送らない**（「🔀 別のキャリア」として一覧に出す）。Salesforceにまだ無い案件は、これまでどおり「違う値は上書きしない」を通す。
  ネットの項目は**全部**、電気・ガスの項目は**どれか**が合えばよい。値は NFKC＋空白抜きで**頭が同じ**なら合う（`東京ガス_(E)`・`ニチガス_(G)` があるため）。
  2本目からの投入は `見分け方を使わない`（`sf_ui.NO_MATCH_KEY`）で外せる（HTB不備のように付箋添付者＝こちらで入れる項目の投入）。
  画面は `sf_ui.render_carrier_match`（キャリアの設定の「5. Salesforceへの投入」）。読めなければ送らずに止める。
  「🔎 シートの案件から当てる」（`salesforce_loader.guess_carrier_match`）：投入用シートの案件を読み、商品が8割以上入っていればネット（商品・エントリー先の5%以上の値）、
  そうでなければ電気・ガス（偏りの大きい方のいちばん多い名前＋1割以上の名前。もう片方も同じ名前なら足す）を候補にする。候補は選択肢の書き方にそろえる。人が見て保存する。
- 🛡 **送らなかった行（上書きしなかった・別のキャリア）も、きょうの投入エラーの欄に出す**（`share_errors(..., held=)`）。どのPCからも見返せる。
  ⭐ **「すでに違う値が入っていた」行は、失敗と同じ表に理由（Salesforceの今の値）つきで出し、件数にも数える**（`sf_ui._held_rows`）。
  ⚠️ 前は折りたたみの中だけで、失敗が0件だと「エラーはありません」に見えていた。対応済みにもできる（その項目を次から送らない）。別のキャリアは折りたたみのまま（`_render_held`）。
- ✅ **「対応済み」にした失敗は、次からその項目だけ送らない**（`salesforce_loader.apply_acks` / `blame`・覚えは予約行 `__progress_ack__`＝`{投入の名前(キャリア／シート): {照合キー: {項目: 値}}}`）。
  キャリアのデータは直せないので、Salesforceを手で直しても毎日同じ失敗が出続けていた（JAPAN電力の電力エントリー済＝連動選択リストなど）。
  覚えるのは「照合キー＋項目＋送ろうとした値」だけ（項目が決まらない失敗は行まるごと＝中身の**指紋**だけ覚える）。
  ⭐ **覚えは勝手に消える**：値が変わった日・シートに出てこなくなった日に忘れる（新しいことが起きたら、また失敗として出す）。
  ⚠️ 選択リストのエラーは項目名が入っていない（`…不適切: ジャパン同意済`）。`blame` が送ろうとした中身から、その値を持つ項目を探す（1つに決まるときだけ）。
  画面は `sf_ui.render_today_errors`（進捗反映の実行の枠の下）：案件ID（文字のまま。セルを選んでCtrl+C。⚠️ 表の外に別のコピー欄は置かない＝担当者の希望。⚠️ リンクにするとタブが次々開くだけ＝担当者は検索で開く）／どの項目（日本語）／送ろうとした値／何が起きたか、の4列＋チェック。
  「元に戻す」一覧もここ。`push_carrier_load(..., supabase=)` を渡したときだけ効く（画面・時間指定の両方で渡している）。
- ☁️ **きょうの投入エラーは、どのPCからも見られる**（`intake_runner.share_errors` / `shared_errors`・予約行 `__progress_errors__`）。
  ⚠️ `_投入エラー/` のファイルは実行したPCにしか残らず、自動実行用のPCの失敗が別のPCでは「失敗1件」としか分からなかった。
  持つのは**きょうの分だけ**（担当者の判断。キャリアごとに最新の1回で上書き、成功したら消す、日が変われば捨てる）。
  送ろうとした中身（お客様のデータ）は入れない。画面（`pages/3_…`）と時間指定（`auto_jobs.run_progress`）の両方から呼ぶ。
- 🔎 **確認用シートをまとめて見る**：キャリアごとに開き直さなくても、
  **貼り付け先スプシ（LL／ネット）ごとに、件数の一覧**が出る。
  0件なら見る必要がないと分かり、**中身があるものだけ**折りたたみを開いて確かめる。
  1キャリアずつ読むので「🔄 ぜんぶ読み込む」を押したときだけ読む（`_read_sheet_rows`・120秒キャッシュ）。

## 🗃 データローダー自動化

⭐ **投入の設定は、どの画面でも `sf_ui.load_editor` 1つ**（データローダー・SMS・オートコール・エントリー後の投入・進捗反映の1本目も2本目も・開通反映）。
⚠️ 前はデータローダー・進捗反映の1本目・開通反映が別の作りで、「ここでは設定できない」がばらばらにあった（担当者の指摘 2026-09-21）。**設定を足すときは `load_editor` に足す。画面側に写しを書かない。**
📏 数の言い方：案件（行）＝「件」（投入結果は `sf_ui._what` で「案件 8件」）／マッピング＝「項目」／投入の設定の数＝「本」／シート＝「枚」。

`pages/7_🗃_データローダー自動化.py`。**ジョブ**（＝このスプシの、このシートたちを更新して、
この投入をする、のひとまとまり）を登録し、「実行する」で ①更新 → ②Salesforce投入 を通す。
設定は Supabase の予約行 `__dataloader__` の `config_json.jobs`。

- **①更新**は SMS送信と同じしくみ：共通ロボット1台 ＋ `sms_runner.run_sheet_refresh(..., url=タブURL)`。
  作業フォルダは `取り込みファイル/データローダー/<ジョブ名>`（`sms_runner.work_dir`）。
- **②投入**は Data Loader（デスクトップ版）を動かすのではなく、
  **`salesforce_loader.py` で API から UPSERT** する。
  Playwright はデスクトップアプリを操作できないうえ、進捗反映タブで同じしくみが既に動いているため。
- ⭐ **投入用シートの作り直しは、スプシのGASをそのまま呼ぶ**（`gas/データローダー_実行WebAPI.gs`）。
  `generateNoGuideDL` などの生成ロジックは**書き換えない**（数式に写すと読みにくく、日付の解釈も崩れる）。
  ジョブに `gas_url` / `gas_token` を持たせ、`sms_runner.run_gas_action` で叩く。
- ⭐ **「▶ ぜんぶ実行する」**：①更新 → ②GASで作り直し → ③目視確認 → ④全件投入 を一気に通す。
  **③で1件でも出ていたら、投入せずに止める**。④は取り消せないのでチェックを入れないと押せない。
- ⭐ **契約Packに合わないフラグの点検**（スプシのGAS `generatePackFlagDL`）：
  契約Pack内容（例「NET/電気/ガス」）＝**案内してよい商材**。そこに無い商材のフラグに
  値が入っていたら誤り。**担当者が `新規`＝まだ案内していない**ので直す
  （電気・ガスは空に／ネットは「案内不要」＋「店舗指示で案内不要」、依頼日は残す）。
  **担当者が名前＝もう案内している**ので直さず `フラグエラーリスト` に出す。
  あわせて、契約Packに入っている商材の依頼日（＋案件依頼日）がそろっているかも見る。
  ⭐ **空いているだけなら、入っている日付でそろえて DL に出す**（人の手を借りない）。
  ⚠️ **日付が2種類以上あるときは、どれに合わせるか決められないので直さず**
  `フラグエラーリスト` に出す。
  ⚠️ そろえた日付を出力に載せるため、`案件依頼日` と `依頼日` も
  **直したあとの値（`P()`）を通す**こと（`D(row[...])` のままだと、直しても出力に出ない）。
  ⚠️ 直した結果を入れる `依頼日契約パックDL` は **`空も送る` をONにしてある**ので、
  GASは**17列すべてを今の値ごと書き出す**（直す列だけ書き替える）。
- ⭐ **「目で見て確認するシート」**（`job["watch_tabs"]`）：`検討エラーリスト` のように、
  **出方が決まっていないので自動判定できない**シート。投入はせず、実行時に読んで
  **2行目以降に何かあれば中身を表で出し、投入の前で止める**（`watch_block`、既定ON）。
  対応して**0件になれば**先へ進む（`watch_block` がOFFなら、出ていても進む）。
  🧹 昔あった「確認して対応しました」のチェックは**外した**。消し込みができるように
  なって役目が終わり、押しても押さなくても同じ欄になっていたため
  （何もしない欄は、迷わせるだけで害になる）。
  ルール化できないものを無理に自動判定させず、「人が見る」を工程として残すための仕組み。
  ⭐ **1件ずつ消し込める**（表の「✅ 対応した」→「🗑 対応した分を消す」→
  `sms_runner.delete_rows_matching`）。担当者は**Salesforce側で直す**ので、
  スプシの行を消すのは**その日の作業の目印**。翌日は①の更新でレポートが最新になり、
  ②の作り直しでエラーリストが作られるので、直っていれば自然に出なくなる。
  ⚠️ **消す直前にシートを読み直し、中身がそっくり同じ行だけ**を消す
  （画面に出したときの行番号で消すと、その間に誰かが行を足した／消したときに
  **別の行を消す**）。見つからなければ消さずに件数を返し、画面で知らせる。
  ⚠️ 連続する行はまとめて、**下から**消す（上から消すと下の行がずれる）。
  消した中身は `取り込みファイル/データローダー/<ジョブ名>/消し込み記録.json` に残す
  （消すと何を消したか分からなくなるため）。
- ⭐ **契約Packに合わないフラグの点検（`generatePackFlagDL`）には、もう1本ある**：
  **状況が「新規」なのに担当者に名前が入っている**行を `フラグエラーリスト` に出す
  （まだ動いていないはずなのに担当がついている＝食い違い）。
  ⚠️ **店舗/顧客名に「東宝ハウス」を含む案件は、この点検から外す**（運用が違うため）。
  契約Packの点検と違い、**Packに入っている商材も含めて全件**見る。
- ⭐ **店舗/顧客名に「Lifeap」を含む案件は、どの契約でも〇**（`PACKDL_FREE_SHOP`・大文字小文字を問わない）。
  契約Packの点検と依頼日の点検から外す。「新規」なのに担当者、の点検は行う（2026-09-21・担当者の判断）。
- 🔔 **Slack通知は「中身が変わったときだけ」**（スプシのGAS `enkanNotifyIfChanged_`）。
  ⚠️ エラーリストは実行のたびに作り直すので、素直に書くと**走らせるたびに同じ通知が飛ぶ**。
  投入でつまずいて押し直すと、1分に何通も届いて**だれも読まなくなった**（実際に起きた）。
  前回の中身をDocumentPropertiesに覚えておき、変わったときだけ送る。
  0件になったら覚えたものを捨てる＝**次に出たときは、ちゃんと知らせる**。
  検討エラーリスト（`checkerror`）とフラグエラーリスト（`packflag`）の両方で使う。
- ⭐ **設定は「シート名 × マッピング」だけ**。`投入設定` のような別表への登録はさせない
  （ジョブにスプシが登録済みなので、同じことを2か所に書くことになる）。
  `job["loads"]` の1件＝`{"シート", "オブジェクト", "照合キー", "マッピング"}` で、
  マッピングも Supabase のジョブの中に持つ。設定スプシには依存しない。
- いま使っている **Data Loader の .sdl マッピングファイルはそのまま取り込める**（`salesforce_loader.parse_sdl`）。
- ⭐ **マッピングに書いた列だけを送る**（`salesforce_loader.build_records`）。
  シートに何列あっても、書いていない列は触らない＝他の項目を上書きしない。
  **値が空のセルはその項目ごと送らない**（＝Salesforce側の既存値が残る）。
  もとは進捗反映のための決まり。届かなかった項目を空で送ると、**前から入っていた
  正しい値を消してしまう**ので安全側に倒してある。
- ⭐ ただし「**意図して消したい**」ときのために2つある：
  - **`空も送る`**（投入ごとのチェック「空欄はSalesforceの値を消す」）…
    **Data Loader を手で動かしたときと同じ**動き。
    ⚠️ **投入ごとに決める**こと。全体で切り替えると、進捗反映のように
    「届かなかった項目は触らない」が前提の投入で、正しい値が消える。
    ⚠️ ONにした投入では、**シートに全列を書き出す**必要がある
    （1列でも空のままだと、その項目が消える）。
  - **`#空` と書いたセル**（`salesforce_loader.CLEAR_MARK`）…
    その1項目だけ空を送って消す。投入全体の動きは変えたくないときに使う。
- 🛡 **すでに違う値が入っている行は送らない**（投入ごとの `違う値は上書きしない`＝`salesforce_loader.NO_OVERWRITE_KEY`。**無ければON**）。
  送る直前に Salesforce の今の値を読み（`find_conflicts`・SOQLで読むだけ）、空・同じ値なら入れる。
  **違う値が1項目でもある行は、その行ごと送らず**結果の一行とCSV（`取り込みファイル/上書きしなかった/`）に出す（`sf_ui._hold_conflicts`）。
  ⚠️ 付箋・進捗反映のように「空の所に入れる」投入で、もともと違う値を書き換えていた（担当者の相談 2026-09-21）。
  ⚠️ 今の値を読めなかったら**送らずに止める**。`#空`・「空も送る」の空（意図して消す）と、Salesforceにまだ無い行は食い違いにしない。
  比べるときは表記ゆれを吸収する（数値・日付・複数選択の並び・15桁/18桁のId）。
  **上書きが仕事の投入はOFF**にしてある（2026-09-21に担当者と決めた：案内不要落とし・依頼日契約パックDL・情報確認総務・海外案件・地点DL・
  エントリ―DL・SMSの投入全部・トス表いつでもリスト）。**ONのまま**：進捗反映（1本目は `__progress__` の `carrier_no_overwrite`）・不動産付箋付けDL。
  画面は `sf_ui.no_overwrite_box`（`load_editor` の中＋進捗反映の1本目）に一本化。
  ✏️ **例外：「Salesforceのこの項目がこの値なら上書きしてよい」**（投入ごとの `上書きしてよい条件`＝`salesforce_loader.OVERWRITE_IF_KEY`・`{"項目", "値": [...]}`。`overwrite_if(ld)` → `push_sheet(overwrite_if=)` → `find_conflicts(allow=)`）。
  付箋の投入で `Lc__c`（L-付箋：チェック）が `完了` なら付け直してよい（担当者の相談 2026-09-21）。今の値を読むときに、その項目も一緒に読む。
  画面は `sf_ui.overwrite_if_box`（`load_editor` の中・「違う値は上書きしない」がONのときだけ）。項目の一覧は**マッピングの項目だけ**（全項目は数百あって探せない＝担当者の指摘）。「マッピングにない項目からも選ぶ」で全部。進捗反映の1本目（`push_carrier`）には無い。
- 設定画面に「シートの列を全部出す」チェックがあり、未設定の列を空欄で並べて選べる。
- ⭐ **マッピングは手で書かない**（`sf_ui.guess_mapping`／「🔎 Salesforceから項目を当てる」）。
  Salesforceの項目一覧を引いて、**ラベルが同じ**ものを当てる。
  表記のゆれ（`ガスNG･案内不要理由` の半角`･` など）は `_norm_label` で吸収する。
  ⚠️ **似ているだけのものは当てない。** 間違った項目に入れると、
  関係ないところを上書きして戻せない。
  ⚠️ **同じラベルの項目が複数あるときも当てない**（`ネット状況` は
  `StageName` と `StageName2__c` の2つある）。候補だけ出して人に決めさせる。
  `ID` だけは名前で当たらない（SF側のラベルは「案件 ID」）ので決め打ち。
  ⚠️ 表は**マッピングが空でも出す**。空のときこそ使いたいのに、以前は
  `if mapping:` の中にあってボタンにたどり着けなかった。
  この表は `sf_ui.mapping_editor` / `mapping_dict` に一本化してあり、
  **進捗反映・開通反映のマッピングも同じ**になる（`sf_ui.render` / `render_carrier_sf` は消した）。
  片方だけ直すと食い違うので、直すときは必ずこの2つの関数で直すこと。
  ⚠️ **マッピングの列名が、そのシートの見出しに無い**ことが実際にあった
  （別キャリアの設定を写したまま直していない等）。そのままでは投入時に止まるので、
  設定画面で**その場で名指しし、シートの実在する見出しを並べて出す**。
- **「🩺 シートと照らし合わせる」**：マッピングの列名がそのシートの見出しに実在するか、
  項目が Salesforce に実在するかを、投入前に確認できる。
  実行時（`_push_one`）も同じ2つを必ず確かめ、欠けていたら**送らずに止める**（誤った上書きは戻せない）。
- お試し（件数制限）→ 全件、の順に押せる。全件はチェックボックスで確認してからでないと押せない。
- 📭 **投入するシートが0件なら「0件のため投入なし」＝失敗にしない**（`sf_ui._zero_result`／`push_ok`）。
  見出しだけ・マッピングの列がどれも空の行しか無い・シートが空、のとき。Salesforceにはつながない。
  ⭐ **照合キーの入った行が1つも無いときも同じ**（`build_records` が0件）。
  ⚠️ ここは「⚠️ 投入できる行がありません（照合キーが空）」で返していたので、
  0件の日の投入（海外案件・地点DLなど）が毎回「失敗」になっていた（時間指定の自動実行で実際に起きた）。
  ⚠️ 前は「⚠️ 投入できる行がありません」で返していたので、エラーリストが空の日などに
  全部実行・時間指定が毎回「失敗」になっていた。**列が無い（設定ミス）は0件でも止める**。
  `push_sheet`（データローダー・SMS・オートコール・エントリー後の投入）と `push_carrier`（進捗反映）の両方に入れてある。
  時間指定の進捗反映では `⏹`（やることが無かった）。
  ⚠️ データローダーの画面に `push_sheet` の写し（`_push_one`）があり、片方しか直らなかった。いまは呼ぶだけ。
- ⚠️ **投入の設定の入力欄のキーは、行ごとの目印（`_uid`）に結びつける**（レポート更新と同じ）。
  番号で付けていたので、「投入1」を🗑で消すと下の投入が番号を引き継いで前の値のまま出て、
  **消したのに消えない**（最後の投入が消えたように見える）になっていた。`_uid` は保存のときに外す。
  🗑 は**「💾 このジョブを保存」で確定**する。
- `_jobs()` に旧形式（`loads` が投入名の文字列）の読み替えを入れてある。

## 📞 オートコール投入（ブルービーン）

`pages/8_📞_オートコール投入.py`。設定は Supabase の予約行 `__autocall__` の `config_json.jobs`。

⭐ **データローダー自動化とは別の業務**。道具がちょこちょこ重なるだけなので、
**画面は分けて、中身の部品は共有する**。担当者の言葉で「別業務」なら、画面も別にする
（同じ台帳に混ぜると「どっちに登録するんだっけ」が起きる）。

  ①更新 →（②GASで作り直し）→（③目で見て確認）→ ④ブルービーンへ投入 →（⑤Salesforce投入）

- 📁 **一覧はフォルダで分ける**（ジョブの `folder`。既定の選択肢は `TS用`／`総務用`、無ければ「未分類」）。
  見え方だけで動きは変えない。各カードの「📁 フォルダを移す」で設定画面を開かずに移せる。
  ⚠️ 開いているフォルダは `ac_folder_keep` にも持つ（部品の値は別の画面へ移ると捨てられ、
  実行から戻るたびに最初のフォルダに戻るため）。
- 🗑 **フォルダをまとめて実行するボタンは外した**（2026-09-15・担当者の判断で不要）。ジョブごとの「▶ 全部実行」「🔧 個別実行」で行う。
  ジョブをまたいでログイン1回で入れる `auto_jobs.autocall_pairs` は、`autocall_many`（1ジョブの全シート）の中身として残っている。

- 💾 **設定の保存は `_save_latest`（書く直前に読み直して、触ったところだけ変える）**。
  ⚠️ 画面を開いたときの `cfg` を丸ごと保存すると、そのあいだに別のタブ・PCで足されたジョブを消す
  （「SB＆BIG-決済or身分証未対応案件」が実際に消えた。2026-09-15。控えにも残っておらず、実行ログから組み立て直した）。
  ジョブの保存は同じ位置で差し替え、同じ名前のジョブがあれば保存しない。「🗑 このジョブを消す」は確認のチェックを入れないと押せない。
  `_save(cfg)` を直接呼ぶ所を増やさないこと。
- 🛑 **①更新・②作り直しが失敗したら、全部実行はそこで止める**（画面の「▶ 全部実行」も時間指定の `run_autocall` も）。
  ⚠️ 画面側は①の結果を見ずに④投入まで進んでいた（2026-09-21に気づいた）。古いリストを入れて同じお客様に二度かけるため。
  個別実行で①が失敗したままなら、④に警告を出す。
- ⭐ **ほとんどのジョブは④だけ**。②と⑤は**トス表作成のような一部のジョブだけ**使う。
  だから工程はすべて任意で、空なら黙って飛ばす。
- ⭐ **CSVは作り直さない**（`sms_runner.fetch_from_gas(..., root=AUTOCALL_ROOT)`）。
  電話番号の頭の0や文字コードの整形はスプシのGASが持っているので、
  `action=csv&sheet=…` で**できあがりを受け取る**だけ。
  ⚠️ 同じ整形をアプリにも書くと、片方だけ直して必ず食い違う（SMS送信で決めた方針と同じ）。
- ⭐ **1シート＝1回の投入**。CSVの置き場はシートごとに分ける（`sheet_slot`）。
  🔐 **ジョブの中の全シートを、ブラウザ1回・ログイン1回で続けて入れる**（`_do_autocall_many` → `sms_runner.run_autocall_rounds` →
  `robot.py --rounds <JSON>`＝`run_robot(rounds=, keep_going=True)`）。⚠️ 前は1枚ごとにロボットを起動し直し、シートの数だけログインしていた。
  周ごとに `has_critical_error`／送信の記録を戻し、結果を `周の結果.json` に1周ずつ書く（途中で落ちても、どこまで済んだか分かる）。
  1枚が止まっても次へ進むが、**ログイン切れ・パスワード欄が出た・画面が閉じた**ときは残りを止める。2周目以降はログイン画面を開き直さない。
  ログは `🔁 i/n：` で周ごとに切り分けて、シートごとの結果に付ける。
  🔁 **止まったシートだけもう一度**（結果の下のボタン）。⚠️ 本番で**インポートまで進んで止まった**シートは投入済みかもしれないので、
  人が一覧で確かめてチェックしたときだけ入れ直す（重なると処理失敗）。お試しの結果なら、お試しでやり直す（結果の `本番`）。
  ⚠️ robot.py は最初の Supabase 読み込みで 504 等が出たら、待って読み直す（生のエラーで落ちていた）。
  🌐 **GASが混んでCSVを受け取れない（HTTP 404／429）**：全部実行で、1枚だけ40秒呼び直しても404のまま止まり、あとで押し直したら通った（2026-09-14）。
  完全自動のため、①`intake_runner.call_gas` は**読むだけの csv**（作り直しなし・Driveに書かない）に限り約6分まで待って呼び直す
  （作り直しを走らせる呼び出しと inspect は短いまま）。②それでも駄目なシートは、`_do_autocall_many` が**ほかのシートを入れ終わってから**
  もう1回だけ受け取り直して入れる（CSVの前で止まった＝ブルービーンには何もしていないので重ならない）。
  つまずきは `取り込みファイル/GASの呼び出し記録.log` に1行ずつ残す（入口 script.google.com で断られた＝スクリプトは動いていない／
  結果の受け取り googleusercontent で断られた、を見分ける。⚠️ URLには合言葉が入るのでホスト名だけ）。原因はGoogle側で、アプリからは見えない。
  📭 **CSVが0件ならロボットを動かさず「📭 投入なし（リストが0件）」**（`_ac_zero_result` の `投入なし`）。
  見出しだけのCSVを入れるとブルービーンでエラーになり、毎日0件のシートがあるジョブが失敗表示になっていた。
  ⚠️ **0件だったことは、表の中だけで済ませない**。実行画面は表の下に
  「📭 リストが0件だったので、投入しませんでした：シート名…」と名指しで出し、全部実行・Slackの
  まとめ（`run_autocall` の `body`）も「📭 0件のため投入なし」と書く。
  黙って✅にすると、入れたのか入れていないのかが分からないまま先へ進んでしまう。
  0件の日も**前に入れたファイルは消す**（下の「🗑 投入の前に」）。
  置き場の大分類は `sms_runner.AUTOCALL_ROOT`＝`取り込みファイル/オートコール投入/`
  （`pattern_dir` 等の `root` 引数。既定は `SMS送信用` なのでSMS送信の動きは変わらない）。
- ⭐ **タイトルとプルダウンは、シートごとに登録して実行時に差し込む**
  （`sms_runner.run_autocall_robot(..., variables=…)` → `robot.py --var 名前=値`）。
  録画は**仮の値**でよく、シートが増えても録り直さない（共通ロボットと同じ考え方）。
  差し込む名前は `job["vars"]`（既定 `["タイトル"]`）で、**そのまま設定表の列になる**。
  ⚠️ 手順書の『値』に `{タイトル}` と書いていないと入らない。設定画面にそう明記すること。
- ⭐ **業務（プルダウン）はシートごとに表で選ぶ**（`autocalls[].業務`）。選択肢は
  「🔄 ブルービーンから業務を読み込む」で読んで **`__autocall__` の `bluebean_options.業務` に覚えておく**。
  読み込み直しは**足すだけ**（前に覚えたものを消さない＝ブルービーン側で消えても表の値が残る）。
  読み込みは `sms_runner.read_select_options` → `robot.py --run … --read-options 業務`：
  **手順書どおりにログイン・移動して、対象が『業務』の選択手順に来たら選択肢を書き出して終わる**
  （何も選ばない・投入しない。ログインの手順を別に持たないため）。
  実行時は名前 → 番号（value）にして `--var 業務=` で渡す。業務が空の行は**動かさずに止める**
  （録画のときの業務に投入してしまうため）。
- ⭐ **作業グループ（ACD）は選ばせない**。業務を選ぶと1つだけ出てくるので、手順書の値を
  **`出てきた1つを選ぶ`**（`robot.ONLY_OPTION_WORDS`）にしておくと、そのとき出ている1つを選ぶ。
  ⚠️ **2つ以上出ていたら選ばずに止める**（どれかを機械に選ばせると、違うグループに投入しても気づけない）。
  手順書が録画のままなら、設定画面の「🔧 手順書を、業務をシートごとに選べる形にする」で直す。
  ⭐ **業務によっては2つ以上出る**（総務（不備解消・後追い）→ `PD不備解消（総務）（8027）` など）。
  **作業グループは業務で決まる**ので、**業務の決まり**（`sms_runner.acd_for`／`DEFAULT_ACD_RULES`＋`__autocall__` の `acd_by_gyomu`）で
  選ぶ名前を引き、`--var 選ぶ:作業グループ=…`（`sms_runner.ACD_PICK_VAR`＝`robot.PICK_VAR_PREFIX`＋名前）で渡す。
  決まりの無い業務は、これまでどおり `出てきた1つを選ぶ`。手順書の値は変えない。名前は NFKC＋空白抜きで比べ、番号（value）でも当たる。
  ⚠️ 最初はカードごとの欄（`autocalls[].作業グループ`）にしたが、「総務なら毎回同じ」なので担当者の指摘で業務の決まりに変えた
  （2026-09-15）。カードに残っている `作業グループ` は**読まない**（保存し直すと消える）。
  決まりは設定画面の「覚えている業務の一覧と、業務ごとの作業グループ」の表で直す（全ジョブ共通）。共通ロボットの「お試し」も同じ決まりを使う。
  ⚠️ **その名前が出ていなければ、ほかを選ばずに止める**。
- ⭐ **投入のあと、結果を一覧で確かめる**（操作 `投入結果を確かめる`＝`check_import`）。
  ブルービーンは投入ボタンを押しても結果がすぐ出ず、「顧客情報インポート一覧」の1行に
  処理状態・無効なデータ件数が出る。**無効なデータ件数が2件以上＝エラー**（1件は見出し行で正常）、
  処理状態が『処理失敗』もエラー。`対象`＝見る列、`値`＝許す数（既定1）。
  - 流れ：インポートを押すと「顧客情報インポート照会」（**今回の分だけ**の縦の表・「新規作成しました。」）
    になり、処理状態も無効なデータ件数もそこに出る。⭐ **この画面で待って読む**（`_detail_values`）。
    読み込み直しは画面の「最新の情報に更新」を押す（`robot_config.import_refresh_button`）。
    照会画面が読めなかったときだけ「一覧」（`import_list_link`）を開いて、
    **ファイル名＋インポート日時がぴったり同じ行**を読む（`_import_row(..., exact_when)`）。
  - ⭐ **無効なデータ件数が多くても、「同じジョブの重なり」なら失敗にしない**（`robot._invalid_verdict`）。
    ⭐ **処理状態が『処理失敗』のときも、まず同じ見分けをする**（`_dup_verdict`）。
    後追いリストのように同じお客様が何枚にも載っていると、あとのシートは**全部が重なり＝処理失敗**になる。
    先に入れたシートでもうかかるので、これは失敗ではない（担当者の指摘・2026-09-20）。
    ブルービーンは**すでに入っている番号**を弾くので、後追いリストのように**同じジョブの7枚**に
    同じお客様が載っていると、必ず無効なデータになる。これは想定どおりで、直しようがない。
    そこで件数が多かったときは、照会画面の**「無効なデータ」を受け取って**（`_bb_invalid_text`。
    押すだけ・何も送らない）弾かれた番号を読み、**そのジョブのリスト（CSV）の中に2行以上ある番号**なら
    重なりとみなして通す。**1回しか出てこない番号**が弾かれていたら、番号そのものがおかしいので
    これまでどおり失敗として名指しする。証跡は `無効なデータ_<シート>.csv` に残す。
    - 見るリストは、アプリが渡す（`auto_jobs.ac_job_csvs` → `sms_runner.JOB_LISTS_VAR`＝`同じジョブのリスト`）。
      ⚠️ **1シートに1つだけ**渡すこと。同じ中身のファイル（`送信データ.csv` と `シート名_日時.csv`）を
      二重に数えると、1回しか無い番号まで「重なり」に見えて、**本当の失敗を見逃す**。
      きょうが0件のシートは、控え（`履歴/`）の中でいちばん新しい中身のあるものを1つだけ見る
      （今回動かしていないシートは、ブルービーンに前の分が残っている）。今回0件のシートは前のファイルを消すので相手にしない。
    - ⚠️ 受け取れなかった（リンクが無い・ログインが切れた）ときは、**確かめられないので失敗のまま**。
      そう名指しする（黙って通さない）。
  - ⭐ 渡すCSVの名前は **シート名_日時.csv**（`auto_jobs.ac_make_csv`）。ブルービーンの一覧に出る名前は
    渡したファイル名そのままなので、「送信データ.csv」だと何の投入か見分けられない。
  - ⚠️ **同じ名前のCSVを1日に何回も入れる**（中身は変わる）。「一番上の行」や名前だけで選ぶと
    別の回を掴むので、日時で選ぶ。二重投入の防止は**入れない**（同じ名前の再投入は正しい使い方）。
  - ⚠️ **`page.reload` は使わない**。POSTの直後の画面を読み込み直すと、**もう一度投入する**。
    一覧に移ってから `page.goto(page.url)`（ただの表示）で開き直す。
  - ⚠️ **送り終わる前に画面を移ると、投入が取り消される**。ファイルを選ぶ欄が消えるまで待ってから動く。
  - お試し（送信しない）では飛ばす。**止めても投入は済んでいる**（取り消せない）。失敗を名指しするためのもの。
  - 足すボタンは `common_robots.import_check_block`（共通ロボットの登録画面とオートコール投入の設定画面の両方から呼ぶ）。
  - ⚠️ **完了画面の文字（`success_text`）は使わない**。この確認で一覧へ移るので、最後の画面に
    「新規作成しました。」は残らず、正常でも失敗になる。登録画面でも入力欄を出さない。
- ⭐ **共通ロボットの登録画面（送る系）は、役割ごとに言い分ける**（`ROLES[...]` の `act` /
  `button_example` / `submit_re` / `precount` / `success_text`）。プッシュプロ＝送信・送る前に
  エラーレコード件数・完了の文字。ブルービーン＝投入・**送る前の件数は無い**（画面に出ない）・投入後に一覧で確認。
  お試し実行もブルービーン用に、オートコールのCSVと {タイトル}{業務} の値を渡す。
- ⚠️ **ブルービーンの投入ボタンは「インポート」＝送信の字が無い**。送信らしい手順の見張り
  （画面の `submit_re`／`robot.unmarked_submit_steps` の `AUTOCALL_SUBMIT_WORDS`）に足してあるが、
  **名前そのものが「インポート」のときだけ**。部分一致にしたら、メニューの「顧客情報インポート」
  「新規顧客情報インポート」まで送信あつかいになり、「1つだけ残す」で**画面移動の手順を消す**
  ボタンが出た（実際に画面で出た）。
- 🗑 **投入の前に、前のファイルを確認なしで消す**（投入の流れの一部。`auto_jobs.ac_prepare` が周ごとに
  **削除モード=探して削除**＋`削除の業務`／`削除のシート` を渡す → `robot.py` の『前回のファイルを削除』＝ログインの直後の手順）。
  同じデータが入っているとブルービーンは**処理失敗**になるため。
  ⭐ **人の確認は取らない**（2026-09-15・担当者の判断）。まだかけられるリストも、今日入れたファイルも、見つけたら全部消す。
  ⚠️ 経緯：①投入の中に「消して入れ直す」チェック（`redo_check`）→ ②別のボタン「🗑 過去リスト削除」（探す→人が選ぶ→消す）に分けた
  → ③**時間指定の自動実行で最後まで通せない**ので、確認なしで投入の流れに戻した。別の画面・ボタンは残していない。
  - 探し方（`_bluebean_find`）：顧客情報インポート一覧の**3ページ目まで**から「**業務が同じ**＋**ファイル名から日時・連番を取った名前が
    シート名と同じ**（`_bb_file_base`）＋削除済みでない」。候補は `削除の候補_<周>.json` に残し、ログに1件ずつ出す。
  - ⚠️ **シート名の `/` などは、ファイル名では `_` になっている**（`robot._bb_name_key` で両方をそろえて比べる）。
    そのまま比べていたので `ドコモ/SB/BIG 工事日未確定案件` の前のファイルが見つからず、消さずに入れて処理失敗になった（2026-09-21）。
  - ⭐ **ほかのジョブのリストも一緒に消せる**（**時間指定の予定**の `also_delete_jobs` → `auto_jobs.run` がジョブに `ALSO_DELETE_KEY` を付ける →
    `ac_also_delete` → `--var 一緒に消すシート=<JSON>`＝`robot.ALSO_DELETE_VAR`）。
    朝の新旧リストには昨日の当日リストと同じお客様が入るので、当日の分が残っていると処理失敗になる（担当者の相談 2026-09-21）。
    ⚠️ **ジョブの設定には置かない**：営業中に画面から新旧リストを入れるときは、当日のリストはまだかけているので消さない（最初はジョブに置いて、担当者の指摘で予定に移した）。
    **業務が同じシートだけ**（N新旧 → N当日）。探し方・消し方は同じ（`_bluebean_find(..., also=)`）。
  - ⚠️ **業務が空のシートは動かさない**（業務で絞らないと、ほかの業務の同じ名前のリストまで消す）。
    **手順書に『前回のファイルを削除』が無いロボットも動かさない**（`ac_prepare` が止める。`robot.run_robot` も起動前に止める）。
  - 消している途中で止まったら、**そのシートは投入しない**（一部だけ消えていることがある）。次のシートへは進む。
  - お試し（`--submit` なし）は**探して名前を出すだけ**で消さない。
  - 結果の表に「消した前のファイル」の件数（`auto_jobs.ac_deleted_count` がログから拾う）。
  - 📭 **CSVが0件のシートも、本番では前のファイルを消す**（`--var 削除だけ=1`＝`robot.DELETE_ONLY_VAR`。消したらその周を終え、投入はしない）。
    ⚠️ 前は消さずに残していたので、もう対象でないお客様にかけてしまう（担当者の判断 2026-09-21）。消せなかったら失敗にする。お試しではロボットを動かさない。
  - 「🔁 止まったシートだけもう一度」は、インポートまで進んで止まったシートもそのまま入れ直せる（入れる前に消すので重ならない）。
  - 消し方は担当者に教わった手順そのまま（`_bluebean_delete`）：照会画面（`/admin/upload_files/view/<ID>/1`）→
    発信リストのリンク → 顧客情報データ一覧 → 検索結果で発信リストを作成 → 名称「削除用_エンカンAI」で保存 →
    そのリストを削除 → もともとの発信リストを削除 → 照会画面で「顧客データを削除」「ファイルを削除」→ 削除済みを確認。
  - 🛑 **数字が読めなければ止める／作成画面の「条件に一致する総件数」が発信リストの全件数と違えば作らない**
    （別のお客様を巻き込むため）。⚠️ 最初は数字が読めないまま削除まで進む穴があった（`_detail_values` が
    「処理状態」の表しか返さなかった）。試験用の画面で見つけて塞いだ。
  - 確認の小窓は `BB_DIALOGS` の3つの文だけOK。
  - ⚠️ 実機で出たつまずき：発信リスト照会の見出しに「？」の説明アイコンが付き、数字が読めなかった
    （`_detail_values` で末尾の？を落とす）／処理失敗のファイルには「顧客データを削除」のボタンが無い（無ければ飛ばす）。
  - 🚧 ブルービーン本体では、削除をまだ最後まで通していない（試験用の画面でのみ確認）。
  - ロボットに手順が無ければ、設定画面が「🗑 前のファイルを消す手順を足す」（`common_robots.bb_delete_block`）を出す。
    削除モードを渡さない実行（共通ロボットのお試し・業務の読み込み）では、この手順は何もしない。
- ⚠️ **ブルービーンのログイン画面は録画できない**（古い Prototype.js が録画の仕掛けを壊し、
  パスワード欄が反応しない・戻る→進むで入力できても記録されない）。ログインの3手順
  （`#AdminUserUsername` / `#AdminUserPassword` / `input[type=submit][value='ログイン']`）は
  **手順書に直接足してある**。「✨ 手順書を作る」で作り直すと消えるので注意。
- ⚠️ **目で見て確認するシートの画面は `watch_ui.py` に一本化**した。
  データローダー自動化（`pages/7_…`）と**同じものを使う**。
  2か所に書くと片方だけ直して食い違う（このアプリで何度も踏んでいる）。
  直すときは `watch_ui.render` だけ。

## 📣 イレギュラー報告（`pages/12_📣_イレギュラー報告.py`）

イレギュラー対応のあった案件を**1件ずつ**出して報告文を書き、「報告」シートに**足していく**画面。
設定は Supabase の予約行 `__irregular__`（`sheet_url` / `wait_tab` / `report_tab` / `done` / `last_refresh`）。

- ⭐ **報告シートには `append_row` で足すだけ**（前に書いた分は触らない）。RAW で書く。
- ⭐ **備考はそのまま出す**（`st.code` のコピーボタンで必要なところだけコピーできる）。
  列は決め打ちしない（案件ID・個人名**以外**の見出しを備考として出す＝レポートの列が増えても動く）。
- ⭐ **AIは任意の道具**（`✨ AIで整える`）。押したときだけ、その欄の文だけを Gemini に送る。
  元の文は `_back` に残して「↩ 元に戻す」で戻せる。
  ⚠️ **入力欄の中身は `_v` で持ち、キーに版（`_ver`）を付けて作り直す**。
  widget のキーに直接入れ直すと「表示したあとに書き換えた」で落ちる。
- ☁️ **報告を出した案件は、Salesforceに印をつける**（`_mark_submitted`）：A列の案件Idで
  `Opportunity.Field129__c`（ラベル「イレギュラー対応」・選択リスト 未提出／提出済み）を `提出済み` にする。
  ⚠️ **触るのはこの1項目だけ**（ほかは送らない＝上書きしない）。
  ⚠️ 報告シートへの追記は取り消せないので、**追記が済んでから**呼ぶ。失敗した案件Idは `sf_ng` に覚え、
  画面の「🔁 Salesforceだけ入れ直す」で直す（報告を二重に足さないため、足し直しはさせない）。
- 🔄 **更新してから出す**（`auto_jobs.run_irregular`）：SFコネクタ（共通1台）で待ちシートを更新 →
  件数を数える。⚠️ **更新に失敗したら数えない**（古い中身で「0件」と言わないため）。
  画面は「きょう更新したか」を出し、していなければ警告する。
- 🔔 **朝は時間指定の自動実行**（業務の種類 `irregular`）。**1件でもあれば Slack**、0件は `⏹` で通知しない。
  画面から押したときは通知しない（`notify=False`。人が見ているため）。

## ⏰ 時間指定の自動実行（`pages/11_⏰_時間指定の自動実行.py` ＋ `scheduler.py` ＋ `auto_jobs.py`）

⭐ **電源入れっぱなしのPC1台**で、決めた時刻に 進捗反映／SMS送信／データローダー／オートコール投入／SFレポート更新 を動かす。
**人の確認が要らない業務を、人がいない時間に回す**のが基本（担当者の言葉）。

- **動かす役は `scheduler.py --tick`**。タスクスケジューラに「5分おきの見回り」を**1本だけ**登録する（画面の「🖥 このPCを自動実行用にする」＝`schtasks /SC MINUTE /MO 5`・pythonw）。
  ⚠️ **時刻をタスクスケジューラに1件ずつ登録しない**。時刻は Supabase の `__schedule__.items` に持ち、画面で直す（人がタスクスケジューラを触らずに済む）。
- **動くのは `__schedule__.host`（PC名）のPCだけ**。ほかのPCに見回りが残っていても何もしない（二重に送信・投入しないため）。
- 📅 **いつ動かすかは3通り**（予定の `repeat`）：`weekly`＝曜日（`days`・既定）／`monthly`＝毎月の日（`month_days`。`99`＝月末）／`dates`＝決めた日付（`dates`＝`YYYY-MM-DD` の並び）。判定は `scheduler.runs_on` に一本化。`repeat` が無い古い予定は曜日として読む。
  ⚠️ 29〜31日は、その日が無い月は動かない（月の最後に動かすなら「月末」）。決めた日付がすべて過ぎた予定は一覧に「🏁」と出す（`next_date`）。
  いつ動かすか（時刻・曜日・日付）を変えたら `since` を付け直す（今日の日付を足した瞬間に、過ぎた時刻の分が「見送り」で飛ぶのを防ぐ）。
- 見回りは**ログオン中だけ**（タスクの既定）＝ロボットがブラウザを画面に出せる。自動ログオン・スリープなしが前提。
- 1つずつ動かす：**OSのロックファイル**（`%LOCALAPPDATA%\EnkanAI\scheduler.lock`・msvcrt）。落ちても残らない。
- 記録は `__schedule_runs__`（`last_tick`／`running`／`done`＝今日動かした日付／`history`）。**書くのは見回り役だけ**。
  画面の「▶ 動かす」は別の行 `__schedule_req__` に足す（同じ行を両方が書くと消し合うため）。
- **遅れたら動かさない**（`late_min`・既定60分）→「⏭ 見送り」で Slack。PCが止まっていた日に、昼に朝の分を送らないため。
  予定を作った／時刻を変えたのがその時刻より後なら、今日の分は動かさない（`since`。作った瞬間に「見送り」が飛ぶのを防ぐ）。
  ⚠️ 「▶ 動かす」「`--run`」で動かした分は `done` に入れない（朝に試しても、時刻が来たら動く）。
- Slack は `slack_notify`（下の「🔔 Slack通知の送り先」）。失敗・確認待ち・見送りは必ず、完了は `notify_done` のとき。
  画面の警告は、見回り役が書く `slack_ready`（自動実行用のPCで送り先が読めたか）で出す。開いているPCの設定で判断しない。

⭐ **実行の中身は `auto_jobs.py` に1か所**。各ページの「全部実行」も同じ関数を呼ぶ：
SMS送信 `sms_run_all`（`state` に `st.session_state` か dict を渡す）／オートコール `autocall_many`／進捗反映 `progress_intake_one`。
⚠️ ページ側にもう一度書かない（時間指定と画面で食い違う）。直すときは auto_jobs を直す。

⚠️ **時間指定だからといって確認を飛ばさない**。各業務の「どこまで自動で行くか」（`auto_send`/`auto_load`/`auto_push`/`auto_call`）に従い、
OFFの工程の手前で `⏸ 確認待ち` で終わる。**この画面に「確認を飛ばす」設定を作らない**（取り消せない操作のため）。
画面は予定ごとに「⭐ 人の確認なしで最後まで」か「⏸ 途中で止まる」を先に出す（`reach`）。
- データローダー：GASのURLが無いのに投入がある → ⏸（前回の中身のまま送らない。画面では人のチェックで通していた箇所）
- オートコール：①更新が失敗したら止める（古いリストを入れて二重にかけない）
- SMS：`check_tabs`（目で見る確認）があるパターンは**毎回②で止まる**＝時間指定に向かない、と画面で言う
- SMS：送信の手前で止めた（⏸）ときは⑤投入もしない（送っていない人を送信済みにしない。画面の全部実行も同じ関数なので同じ動きになった）
- 進捗反映：有効な全キャリアを「反映して投入」（`push_salesforce` に従う）。手動アップロードのキャリアは ⏸

🚧 E2E未検証（2026-09-15）：本物の見回りでの実行・タスクスケジューラの登録・Slack送信。ロジックはモックで確認済み。

## 🔔 Slack通知の送り先（`slack_notify.py` ／ 画面は「⚙️ その他設定」）

⭐ **URLはPCごとに `secrets.toml` へ書かせない。** 画面で1回貼れば `ENKAN_SECRET_KEY` で暗号化して
Supabase の予約行 `__slack__`（`url_enc`）に入れ、**全PCがそこから読む**（`GOOGLE_OAUTH_CLIENT_JSON` と同じ考え方）。
担当者のPCに少しずつ入れる形だと、入っていないPCだけ通知が来ない。

- 探す順：環境変数（GitHub Actions）→ そのPCの `secrets.toml` → 共有（Supabase）。`webhook_url()` に一本化。
  `robot.notify_slack` も `scheduler.slack` もここを通る。⚠️ 送り先を別に探す処理を書かない。
- ⚠️ 復号できないPC（鍵が無い／鍵が違う）は `webhook_url` が理由を返す。画面はそれを名指しする（黙って通知しないにしない）。
- 行には**読み直して足す**（丸ごと upsert しない）。
- ⚠️ Streamlit を import しない（robot.py・scheduler.py から使うため）。

## 🔎 エントリー前DC（エントリー前の内容チェック）

`pages/9_🔎_エントリー前DC.py` ＋ スプシのGAS `gas/エンカンAI_DC.gs`。設定は Supabase の予約行 `__precheck__`。
流れは **①SFコネクタで貼り付けシートを更新 → ②チェックする（ミスだけを理由つきで出す）**。

- ⭐ **ルールの正本はスプシの「DCルール表」**（1行＝1ルール：ID／ON／対象／種類／ルール名／NGの理由／見る列／
  条件（2行目の形の数式）／メモ／もと／登録日）。条件付き書式は正本にしない
  （数式しか残らず「何を登録したか覚えていない」「一覧が古い」になっていた）。
  最初の53本は条件付き書式から写し、同日に見直して52本（担当者に確認済み）。条件付き書式側は
  **列が1つずれたまま**（G06/G07/G12/G13/G14）・**範囲が崩れて数セルだけ**（N16/G01/G02）・壊れた条件（N17）が混ざっていた。
  ⚠️ 見出しが読めないように見えても、`row_values` の読み違い／レポートの列の出し入れのことがある。**必ず今の見出しを読み直してから**直す
  （E15/E16/G17/G18 は「見出しの無い列」と誤って判断していたが、実際は郵便番号の列だった）。
- ⭐ **判定はスプシの中で行う**（`enkanDcRun`）。⚠️ 条件付き書式の**色はどこからも読めない**ので、
  判定用の隠しシート `DC判定_<対象>` の左にチェック用シートを ARRAYFORMULA で映し、右の列に
  ルールの数式を2行目に置いて下へコピーする（行番号はスプシが合わせる＝数式を読み解いて書き換えない）。
  TRUE の行だけを `DCエラー一覧` に書き出し、隠しシートは消す（残すとスプシが重くなる）。
  - ⚠️ **案件番号の無い行は判定しない**（SFレポートの合計行などが「空なのでNG」になった）。
  - ⚠️ 1行だけ計算できないのはNGではない（生年月日が空の DATEDIF など）。**全行で**計算できないときだけ「⚠️ 式エラー」。
  - ⚠️ **同じ行・同じ見る列でNGがあれば、注意（ギリギリ）は出さない**（期限切れは必ずギリギリにも当てはまる）。
  - 2人が同時に押すと隠しシートを取り合うので `LockService` で1人ずつ。
- 🚦 **受付期限の信号（E11/E12/E19・G10/G11/G19）は担当者の運用に合わせてある**（2026-09-13）：
  エントリーは **LL 18時**（N 16時・ネットの期限ルールはまだ無い）。判定は**チェックした今の時刻**（`NOW()-TODAY()` と `TIME(18,0,0)`・スプシは日本時間）。
  🔴＝期限日**より前**（今日エントリーしてもアウト＝昨日までに要った）／🟡＝**ちょうど期限日**（18時までにエントリーすればセーフ）／
  🟡のまま**18時を過ぎて登録日（電力登録日 M・ガス登録日 AH）が空**なら🔴。**登録日が入っていればどれも出さない**。
  ⚠️ 最初は19時で切り替えていたが、担当者の言葉は「18時前なら可、18時以降なら不可」（18時過ぎにエントリーしようとしているなら、登録日が空なのは当たり前）。
  ⚠️ 締めの前後にチェックすると表記が変わって迷うので、画面の結果の上に**チェックした時刻と「締めまであと◯分／◯分過ぎ」**を出す（`ENTRY_CUTOFF`。数式の18時と必ずそろえる）。
  ⚠️ もとの条件付き書式は「今日エントリーすれば間に合う日」まで赤、「あと1営業日」を黄にしていた（1日早く赤になっていた）。
- ⭐ **新しいルールはアプリから**：日本語 → Gemini が数式（列の一覧と今のルールを渡す）→
  「🧪 いまのデータで試す」＝ `DC試し` に1行書いて `enkanDcTry` → `DC試し結果` を読む → 確認のチェック → 登録。
  ⚠️ シートへは **RAW で書く**（数式として計算させない）。
- ⭐ **「このルールどうなってる？」**：Gemini に数式と列の一覧を渡し、列の名前で説明させる。
- 表で直せるのは ON／種類／ルール名／NGの理由／メモ だけ。**数式は表で直させない**（1文字で全件NGになる）。
  書き戻しは変えたセルだけ。
- ⚠️ 判定をアプリ（Python）に書かない。スプシの数式と二重になり、必ず食い違う。
- ⚠️ 前の `updateAllAndAddNotes`（全ルール一覧を作る）は最後の `alert` を try/catch にしたが、アプリからはもう呼ばない。

## 🔧 GASを、アプリが直接書き込んで公開する（`gas_deploy.py`）

⭐ **コピペをなくす。** これまでは「アプリが出したコードを人がコピー → Apps Script に貼る →
合言葉を書き替える → デプロイ → 出てきた `/exec` を貼り戻す」だった。手順が多く、どこか1つ
抜けると `API_TOKEN が未設定です` で止まる（実際に何度も起きた）。
いまは **Apps Script API** で、アプリが ①いまのコードを読む → ②控えを取る →
③合言葉を入れた連携コードを**専用ファイル**として書き込む → ④新バージョンを作って公開 →
⑤出てきた `/exec` と合言葉を設定に書き戻す、まで行う。**人が貼るのはスクリプトのURL 1つだけ**。

- 画面は `gas_deploy.render(...)` **1か所**。SMS送信（`pages/6_...`）とデータローダー
  （`pages/7_...`）の両方から呼ぶ。⚠️ 画面を2つ書くと片方だけ直して食い違う。
- ⭐ **手で貼る道は残していない**（コードを出す欄・URL欄・合言葉欄は削除済み）。
  やり方が2通りあると、**どちらをしたのか分からなくなり**、合言葉の食い違いと
  古い版の残りが、そのまま事故として残るため。URLと合言葉は
  「🔎 いまの設定を見る」で**読むだけ**。作り直しはチェックを入れて入れ直す。
- 保存する項目：`gas_script_url`（人が貼る）／`gas_deployment_id`（公開先。**URLを変えない**ため）。
  従来の `gas_url` / `gas_token` は**そのまま**（実行のしくみは何も変えていない）。
- ⚠️ **Apps Script API はサービスアカウントでは使えない。** スプシの持ち主のアカウントで
  1回だけ許可（OAuth）をもらう。鍵は `GOOGLE_OAUTH_CLIENT_JSON`（デスクトップアプリの
  クライアントJSONをそのまま）。**そのアカウントで
  https://script.google.com/home/usersettings の Apps Script API をオン**にする必要がある。
- ⭐ **鍵は `secrets.toml` に書かせない。** 画面から貼れば `save_client_json` が
  Supabase の `__gas_auth__` に入れ、**全PCがそこから読む**（`client_config` の探す順は
  secrets → このPCのファイル → Supabase）。⚠️ PCごとに書き写す形にすると、
  **アプリを配るたび全員に同じ書き替えをさせる**ことになる（そこで実際に詰まった）。
  デスクトップアプリのクライアントは、そもそも秘密として持てない種類のもの。
- ⚠️ `__gas_auth__` の行には**鍵（client）と許可（token）が同居**する。
  どちらを書くときも**読み直して足す**こと（丸ごと upsert すると片方が消える）。
  `forget()` も許可だけ消す（鍵を消すと全PCが「鍵がありません」に戻る）。
- 許可は `%LOCALAPPDATA%\EnkanAI\gas_oauth_token.json` に置き、`ENKAN_SECRET_KEY` があれば
  Supabase の予約行 `__gas_auth__` にも**暗号化して**残す（別PC・クラウドから使い回すため）。
  ⚠️ クラウドではブラウザを開けないので、PCで許可を取ってから画面で貼り付ける。
- ⭐ **合言葉はスクリプトに1つ**（`install` がスクリプトに入っている合言葉を読んで使う。作り直しのチェックのときだけ変える）。
  ⚠️ 以前はジョブごとの合言葉で書き替えていたので、**同じスプシを使う別のジョブで押すと、先のジョブが
  「合言葉が違います」になった**。また「＋ ジョブを追加」の名札が毎回同じで、**前のジョブの公開先（別のスプシ）が
  新しいジョブに保存された**。いまは、渡された公開先がそのスクリプトのものでなければ使わず、覚えている結果も
  スクリプトが違えば捨てる。オートコール投入の「🔌 つないで中身を見る」は、返ってきたシート名が
  ジョブのスプシと違えば「別のスプシのGAS」と名指しする。
- ⚠️ **HTTP 403（アクセスが拒否されました）**は、公開範囲が「全員」でも出る。Googleの入口で断られて
  スクリプトは動いていない。持ち主が Apps Script で関数を1回実行して**承認**してから入れ直す（`call_gas` が案内する）。
- ⚠️ **書き込む前に、いまのコードを丸ごと控える**（`取り込みファイル/GASの控え/`）。
  人のスクリプトを触るので、戻せない状態にはしない（`gas_deploy.restore`）。
- ⚠️ **前に手で貼った版が残っていると、同じ名前（`API_TOKEN`／`doGet`）が2つになって
  スクリプト全体が動かなくなる**。エンカンAIの見出しがある分は**自動で外し、外したことを画面に出す**。
  見出しの無い他人の `doGet` を見つけたときは、**書き込まずに中止**して名指しする（勝手に消さない）。
  ⚠️ 外すのは「見出しから下」だが、**新しい連携コードに無い関数は残す**（`_strip_old_block(keep_unless=)`）。
  古い版の下に人が足した `generatePackFlagDL` まで消し、サイドバーが `is not defined` で落ちた（2026-09-20）。
- 公開のしかたは manifest（`appsscript`）で決める：`executeAs: USER_DEPLOYING` ／
  `access: ANYONE_ANONYMOUS`（＝これまでの「自分として実行／全員」と同じ）。
  ⚠️ 既にある manifest は**上書きせず**、`webapp` だけ足す。
- 入れたあと、その場で `check_gas_csv` を叩いて確かめる。返事が無いときは
  「スクリプトを一度も**承認**していない」がほとんどなので、そう案内する。
- ⚠️ 鍵（`GOOGLE_OAUTH_CLIENT_JSON`）が無いPCでは、**用意のしかたを名指しで出して止める**
  （黙って空欄にしない）。すでに保存済みの `gas_url` はそのまま使えるので、
  動いている設定が鍵の有無で壊れることはない。

## 🧰 全ロボット共通のしくみ（`robot.py` に入れる＝どのページからも効く）

ロボットの実行はすべて `run_robot` を通るので、ここに入れた対策は
**エントリー業務・進捗取り込み・SMS送信・データローダーの全部**に効く。
逆に、**画面（司令室・共通ロボット登録）の側は追随が要る**（操作の選択肢・列）。

| しくみ | 何のため |
|---|---|
| `_find_anywhere` | 小窓（iframe）の中まで探す／出るまで待つ（既定90秒）／読点・句点のズレを吸収 |
| `_hidden_link_href` | ⚠️ **カーソルを乗せると開くメニュー**は録画に写らず、動かすときは中のリンクが見えずに止まる（ブルービーン）。クリックが空振りしたら、**文字がぴったり同じで隠れているリンク**の行き先を直接開く。「▶」で横に開くだけの項目（行き先なし＋下にリンク）は飛ばす。録画の仕方は変えなくてよい |
| `_reopen_menu_and_click` | ⚠️ **横に開くメニュー（`Salesforce Connector►`）が途中で閉じ、中の `Open` が大きさ0で押せない**ことがある（SFコネクタ更新の2枚目で実際に止まった）。直前の手順の対象が ► で終わるときだけ、Esc → 手前のクリック（拡張機能）→ ► の項目に**カーソルを乗せて**開き直し、押し直す（`_menu_chain`）。開くだけなので何も送らない。小窓の中まで探す（最大90秒）より前に試す |
| `_marker_on_page` | 目印さがし。小窓の中も見る |
| `目印` 列 | その文字が画面にある日だけ実行（ログイン済みの日に止まらない） |
| `login_step_indexes` | ログイン一式（ID→パスワード→ログイン→認証コード）を**かたまり**で見分ける |
| `_password_box_visible` | パスワード欄が出ているか＝**ログイン画面かどうかの最も確かな手がかり** |
| `_login_needed` | パスワード欄も、手順の欄・ボタンも出ていなければ「ログイン済み」＝丸ごと飛ばす |
| `_looks_logged_in` | 「ログアウト」等が出ている＝もう入っている（待たずに切り上げる合図） |
| `window.close` を無効化 | サイトに窓を閉じさせない（ダウンロード中に消えるのを防ぐ） |
| ダウンロード表示を切る | ⚠️ **Chromeがダウンロードの瞬間に落ちる**ことがある（東急）。`--disable-features=DownloadBubble…` |
| Chromeが落ちたらやり直す | ⚠️ ダウンロードでChromeごと落ちる日がある。**付属のChromiumで1回だけ自動で再実行**（`ENKAN_FORCE_CHROMIUM=1`） |
| `robot_config.browser` | `chromium` にすると、本物のChromeではなく付属のChromiumで動かす（落ちるサイトの逃げ道） |
| 画面から選べる | 進捗反映の「キャリアの設定 → 🌐 使うブラウザ」で切り替え（Supabaseを直接さわらせない） |
| プロファイルを分ける | ⚠️ Chromeの設定はChromiumでは開けない（**ブラウザが開かなくなる**）。`_chromium` を付けて別置き |
| 予備タブを1枚残す | ⚠️ **本物のChromeはタブが0枚になると終了する**。サイトが窓を閉じても生き残らせる |
| `_fetch_pending` | 受け取れなかったファイルを**URLから取り直す**（窓が閉じてもブラウザは生きている） |
| `_looks_like_web_page` | ⚠️ 取り直した中身がHTMLなら**取り込まない**（ログイン切れのエラー画面をExcelとして保存しない） |
| `_pump` | 待つあいだも、ブラウザからの知らせ（ダウンロード開始）を受け取る |
| 画面が閉じたら移る | 別の画面が開いていればそちらへ。無ければ言葉にして止める（生のエラーを出さない） |
| `_close_dialog` | 出ている窓を閉じる（**ページの中の**モーダル。次の操作の邪魔をさせない） |
| `_answer_dialogs` | **ブラウザ本体の小窓**（prompt/confirm）に答える（下記） |
| `open_mail_link` | メールで届く使い切りのログインURLを受け取って開く（下記） |
| `出るまで待つ` / `終わるまで待つ` | 時間のかかる処理の完了を待つ |
| `数を確かめる` | 送る前に件数を確認して、多ければ止める |
| `投入結果を確かめる` | 投入の後、一覧の自分の行（ファイル名＋日時）で件数と処理状態を確かめる。無効なデータが多いときは「同じジョブの重なり」かを見分ける（`_invalid_verdict`） |
| `ページを開く` | 値のURLを直接開く。⚠️ ブルービーンの上の帯のメニューは、押したあと待つ間に閉じて中のリンクが見つからない（実際に止まった）。毎回同じ画面ならメニューをたどらない |
| `ファイルをアップロード` | 手元のファイルを渡す |
| `--each` / `--each-url` / `--var` | 同じ手順を、値や開く先を変えて繰り返す |
| `_looks_signed_out` | ログイン切れを名指しして止める |
| サンドボックス有効 | ローカル実行時は Chrome の安全機構を切らない |
| `profile_path` | ブラウザの設定は **OneDriveの外**（`%LOCALAPPDATA%\EnkanAI\profiles`）に置く |

⚠️ **Chromeプロファイルをアプリのフォルダ（＝OneDriveの中）に置かないこと。**
Chromeは常に細かいファイルを書き換えるので同期とぶつかり、
「既存のブラウザ セッションで開いています」で起動できなくなる（実際に起きた）。
場所の決定は `robot.profile_path` に一本化してある（`common_robots.profile_dir` もこれを呼ぶ）。
昔の場所（`.enkan_profile/`）にあれば、初回に黙って引っ越す（ログイン状態を失わないため）。

⚠️ 新しい操作を `robot.py` に足したら、**`pages/2_...:ACTION_OPTIONS` と
`common_robots.py:_ops` の両方**に足すこと（片方だけだと画面から選べない）。

### 📧 メールからログインするサイト（メールリンク認証／マジックリンク）

「ログインのメールを送る → 届いたリンクを開く → ログイン」という作りのサイト向け。
**ロボットは1台**にまとめ、ログイン部分の手順すべてに `目印` を付ける（ログイン済みの日は飛ばすため）。

| 順 | 操作 | 中身 |
|---|---|---|
| 1 | `クリック` | 「ログインメールを送る」ボタン。目印を付ける |
| 2 | `メールのリンクを開く` | 値＝設定の名前。目印を付ける |
| 3 | （手順にしない） | メアドを聞く小窓には `_answer_dialogs` が自動で答える |

- **受け取りの道は認証コードと同じ**（GAS `fetchAuthCodes` → 設定スプシの「認証コード」タブ →
  `wait_for_auth_code`）。違うのは**取り出し方（正規表現）だけ**なので、
  司令室の「🔐 メールで届く認証」で `ログインのリンク（URL）` を選ぶと、
  貼ったメール本文から `robot_settings_ui.guess_link_pattern` がURL用のパターンを作る。
- ⚠️ **GASは平文とHTMLの両方を見る**。ボタンだけのメールは平文側にURLが載らないことがあり、
  平文だけ見ていると「メールは届いているのに取れない」になる（`&amp;` を戻してから探す）。
- ⚠️ **ログを見た人がログインできてしまう**ので、開いたURLは `_safe_url` で
  合言葉（`oobCode` 等）を伏せてから出す。
- ⚠️ **`目印` が無いと、来ないメールを待ち続けて失敗する**（ログイン済みの日はメールが来ない）。
  司令室の「🩺 設定できているか調べる」が、目印の無い手順を名指しで警告する。

#### 🗨 ブラウザ本体の小窓（`_answer_dialogs`）

Firebase のメールリンク認証は、リンクを開いた先で `prompt()` で
「ログインするためのemailを入力してください」と聞いてくることがある。

- ⚠️ **これは DOM ではない**＝**録画に写らない／手順書に書けない**。`_close_dialog` も効かない。
- ⚠️ **Playwright は、何も用意しないと小窓を勝手にキャンセルする**（実測で確認済み）。
  だから用意しないと、この作りのログインは**必ず失敗する**。
- 🛡 **何でもOKしてはいけない。** 「送信しますか？」に勝手にOKすると、確かめずに申請してしまう。
  `robot_config.dialog_marker`（小窓の文の一部）が入っているときだけ答え、
  **それ以外はこれまでどおり閉じる**。答えは `dialog_answer`（`{秘密:名前}` が使える）。
- 設定画面は `robot_settings_ui.render_browser_dialog_settings`（司令室と進捗反映の両方から呼ぶ）。
  メールを待つ時間（`robot_config.mail_wait_sec`・既定180秒）もここで延ばせる。

## 📊 ホームと「全状況進捗確認」（`status.py`）

⭐ **仮の数字は置かない。** 以前のホームは「本日の処理件数 0 件」を**常に**出していて、
動いているのか止まっているのか分からなかった。集められないものは「—」にして、
なぜ出せないかを画面に書く。

- 集める先は `status.py` に一本化（ホームと確認ページが**同じ数字**を見るため）。
  Supabase の `merchants`（ロボットと各業務の設定）＋ `取り込みファイル/` のログ・記録
  ＋ `artifacts/` のスクショ。
- ⚠️ **ここでスプレッドシートは読まない**（1枚で数秒かかり、開くたびに固まるため）。
  中身の確認は各ページで行う。画面にも「このパソコンでの記録です」と明記する。
- 止まったときのスクショは、ファイル名（`<ロボット>_<できごと>_<日時>.png`）から
  日本語の説明にして出す（`status.SHOT_WORDS`）。`robot.py` に新しい `_save_screenshot`
  のラベルを足したら、ここにも1行足す（無くても英語のまま出るだけで壊れない）。
- ⚠️ `status._has_submit` は `robot.py` を import する（判定を2か所に書かないため）。
  Playwright が入っていないPCでは import できないので、**False と言い切らず None**＝「—」。

## 📘 設定マニュアル（画面を変えたら、ここも直す）

担当者向けの設定マニュアルが **1本だけ**ある。画面の入力欄を増やす・減らす・
文言を変えるときは、**同じ変更の中で**これも直すこと（あとで直す、は必ず忘れる）。

| もの | 場所 |
|---|---|
| マニュアル本体（正本） | `docs/エンカンAI_設定マニュアル.html` |
| 項目の記録 | `docs/manual_items.json` |
| ずれを見つける道具 | `tools/manual_check.py` |
| 公開ページ（Artifact） | https://claude.ai/code/artifact/f2c337fe-49c8-45a1-920f-eb8ceb30a5d1 |

- `tools/manual_check.py --check` が、画面の入力欄（`st.text_input` 等・表の列）を
  数え上げて記録と見比べ、**増えた／消えた項目を名指しする**。
  `.claude/settings.json` のフックで、ファイルを編集するたびに自動で走る
  （ずれていないときは何も言わない）。
- 直し方の手順は **`/update-manual` スキル**（`.claude/skills/update-manual/`）にある。
- ⚠️ **マニュアルを直したあとに** `--save` で記録を更新する。順番を逆にすると、
  直していないのに「ずれなし」になる。
- ⚠️ **新しいマニュアルを作らない。** 上のファイルとURLを更新する
  （別に作ると、担当者が古いほうを見続ける）。
  Artifact の publish には必ず上の `url` を渡す。
- ⚠️ **URLはこのファイルに書いておく。** Claude Code の「記憶」はPCごとに別なので、
  別のPCから作業したときに同じページを更新できなくなる。

## 📁 ファイル構成

```
kannri_app/
├── app.py                    # Streamlit トップページ（今日の様子＋やることの入口）
├── robot.py                  # Playwright 自動操作エンジン（CLI 単体実行可）
├── sms_runner.py             # SMS送信の下ごしらえ（チェック／CSV化／ロボット起動）
├── gas_deploy.py             # GASをアプリが直接書き込んで公開する（コピペをなくす）
├── report_refresh.py         # SFレポート更新（スプシをまたいでまとめて更新）
├── entry_loader.py           # エントリー後の投入（スプシをまたいでまとめてSalesforceへ）
├── status.py                 # いまの状況を集める（ホームと全状況進捗確認が同じ数字を見る）
├── watch_ui.py               # 目で見て確認するシート（データローダーとオートコールで共用）
├── auto_jobs.py              # 業務を画面なしで通しで動かす中身（各ページと時間指定の自動実行で共用）
├── scheduler.py              # 時間指定の見回り役（タスクスケジューラが5分おきに呼ぶ）
├── slack_notify.py           # Slack通知の送り先（画面で1回保存→暗号化してSupabase、全PCで共有）
├── pages/
│   ├── 1_📊_全状況進捗確認.py        # 実行の記録・ロボット・証跡を1画面で
│   ├── 2_📝_エントリー業務自動化.py  # ★中核：ロボット作成ウィザード＋司令室
│   ├── 3_🚀_進捗反映自動化.py        # 進捗反映（実行／確認と、⚙️設定は別画面）
│   ├── 4_✅_開通反映自動化.py        # 準備中
│   ├── 5_🛑_変更キャンセル自動化.py  # 準備中
│   ├── 6_📱_SMS送信.py               # SMS一括送信（パターン別・プッシュプロ連携）
│   ├── 7_🗃_データローダー自動化.py  # シート更新＋Salesforce投入（Data Loaderの代わり）
│   ├── 8_📞_オートコール投入.py      # ブルービーン投入（別業務。GAS実行・投入は任意）
│   ├── 9_🔎_エントリー前DC.py        # エントリー前の内容チェック（ルール一覧＋確認）
│   ├── 10_🎧_通録ダウンロード.py     # 準備中（枠だけ）
│   ├── 11_⏰_時間指定の自動実行.py   # 予定の登録・自動実行用PCの設定・実行の記録
│   └── 99_⚙️_その他設定.py          # 接続キーの確認・ZIP配布・管理メニュー
│       ↑ 99 にしてあるのは、タブを足すたびに番号を振り直さずに済ませるため
├── manual.html               # 利用者向けセットアップガイド
├── requirements.txt          # 依存パッケージ
├── start.bat / start.command # Windows / Mac 用ランチャー（自動セットアップ付）
├── tools/dev-setup.bat       # 開発用：git名義＋gh(GitHub CLI)を用意する
├── tools/key_check.py        # 開発用：鍵が他のPCと同じかを、実際に復号して確かめる
├── README.md
├── .gitignore                # secrets.toml などを除外
└── .streamlit/
    └── secrets.toml.example  # 接続キーのテンプレート
```

`pages/2_📝_エントリー業務自動化.py` がアプリの中核。4つのビュー（`st.session_state.view`）で動く：
1. `dashboard`：ロボット一覧
2. `step1_basic`：基本情報（名前・SFA URL）
3. `step2_record`：Playwright codegen で録画 → Gemini で手順書生成
4. `project_room`：司令室（手順編集・条件分岐・テスト実行）

## 🔑 接続キー（secrets）

`.streamlit/secrets.toml` に以下を設定（`.gitignore` 済み・コミット禁止）：

```toml
SUPABASE_URL    = "https://xxxxx.supabase.co"
SUPABASE_KEY    = "eyJhbGc..."  # anon key
GEMINI_API_KEY  = "AIzaSy..."
# GOOGLE_OAUTH_CLIENT_JSON = '''{"installed":{...}}'''  # 任意：GASをアプリが直接書き込むとき
# SLACK_WEBHOOK_URL = "https://hooks.slack.com/services/..."  # ふつうは書かない：「⚙️ その他設定」で保存すれば全PCで使える（ここに書くとこのPCだけ優先）
# DRIVE_SMS_ROOT = "..."   # 任意：SMS用CSVを置くDriveフォルダID。⚠️ このリポジトリは公開なので、コードに書かない
```

`secrets_check.py` が全ページの先頭で点検し、壊れていたら**直す場所を名指しして止める**
（別PCへコピーし損ねて `"` が欠け、Pythonの生のエラーだけが出た事故があったため）。
⚠️ `ENKAN_SECRET_KEY` は**PCごとに違うと、暗号化して保存したパスワードが読めなくなる**。
手で打ち直さず、動いているPCのファイルをそのままコピーすること。

> 機密情報は絶対にコミットしないこと。`secrets.toml` / `.setup_done` は除外済み。
> `GEMINI_API_KEY` は **手順生成（Streamlit側）専用**。クラウドの申請実行（`robot.py`）では使わない。

## 🚀 開発・実行

```bash
python -m venv venv && source venv/bin/activate   # Windows: venv\Scripts\activate
pip install -r requirements.txt
playwright install chromium
cp .streamlit/secrets.toml.example .streamlit/secrets.toml   # 値を記入
streamlit run app.py
```

ロボット単体テスト：`python robot.py "<ロボット名/プロジェクトID>"`（モック顧客で実行）

ロボットを1回だけ動かす（SMS送信などで使用）：
`python robot.py --run "<ロボット名>" <作業フォルダ> [--submit] [--file <渡すファイル>]`

### 🧰 別のPCで開発をはじめるとき

```bash
tools\dev-setup.bat        # Windows：ダブルクリックでも可
```

- git の名義を `Claude` にする（このファイルの最重要ルール）
- **`gh`（GitHub CLI）が無ければ入れる** → そのPCでも PR を作れる
  （管理者の許可が使えないPCでも入るよう、winget → zipを展開、の順で試す。
  中身は `tools/install-gh-user.ps1`）
- `gh auth login` まで案内する
- `requirements.txt` と **Playwright の Chromium** を入れる（ロボットの実行に要る）
- ⭐ **鍵（`ENKAN_SECRET_KEY`）が他のPCと同じかを確かめる**（`tools/key_check.py`）。
  ⚠️ ファイルが「ある」だけでは分からない。**別の鍵で作り直すと、画面は正常に見えるのに
  実行のときだけ「復号できません」で止まる**。実際に保存済みのものを1つ復号して白黒つける
  （中身は表示しない）。

⚠️ **担当者のPCでは実行しない**（`gh` は開発のときだけ使う）。
担当者向けは `start.bat`（起動）と `update.bat`（更新）のまま。
`--submit` を付けたときだけ『送信（本番のみ）』ステップまで実行する。

## ☁️ クラウド実行（GitHub Actions / 担当者PC非依存）

`.github/workflows/run-robots.yml` が毎日（UTC 23:00＝JST 08:00）＋手動で起動し、
`python robot.py --all` を実行する。担当者のPCを開かなくてもクラウドで動く。

- **鍵の読み込み**（`robot.py:load_secrets`）：環境変数 `SUPABASE_URL`/`SUPABASE_KEY`（必須）が
  あればそれを優先（CI 向け）、無ければ `.streamlit/secrets.toml`（ローカル向け）。
  `SLACK_WEBHOOK_URL` は任意（通知用）。`GEMINI_API_KEY` は申請実行では未使用（互換のため残置）。
- **GitHub Secrets**：リポジトリの Settings → Secrets and variables → Actions に
  `SUPABASE_URL`/`SUPABASE_KEY`（必須）を登録。通知したい場合のみ `SLACK_WEBHOOK_URL` を追加。
- ⚠️ **`_processed_keys` の書き戻しには Supabase の `merchants` 行への UPDATE 権限が必要**。
  anon キーに RLS で UPDATE が無いと保存が黙って失敗し二重申請リスクになる（失敗時は警告ログ）。
- **headless 切替**（`robot.py:is_headless`）：`ENKAN_HEADLESS=1/0` を明示。未指定なら
  `CI` 環境変数があるとき自動で headless（ワークフローは `CI: "true"` を渡す）。
- **実行モード**：`--all`＝稼働中（`is_active=True`）の全ロボットを処理（`run_all_active`）。
  引数にロボット名→そのロボットをモック顧客で単体実行。
- **二重起動防止**：`concurrency` で同時実行を抑止（二重申請の防止）。
- **証跡**：失敗・中止・CAPTCHA 検出時に `artifacts/` へスクショ保存（`_save_screenshot`）。
  ワークフローは `artifacts/` を成果物としてアップロード（14日保持）。
- **ボット検知の安全停止**（`_looks_blocked`）：CAPTCHA 等の壁を検出したら送信せず中止。

## 📄 SFAスプレッドシート連携（リンク共有・読み取り専用／任意でサービスアカウント認証）

`fetch_pending_rows`（`robot.py`）が、スプシから未エントリー行を取得する。

- **接続方式（既定）**：認証なし。スプシの共有を「**リンクを知っている全員（閲覧者）**」にする。
  gviz エンドポイント `…/gviz/tq?tqx=out:csv&sheet=<タブ名>`（`_csv_export_url`）でCSVとして取得。
  非公開だとログインHTMLが返るため、`_parse_pending` が分かりやすいエラーにする。
- **接続方式（任意・認証あり）**：実在の顧客情報を含み「リンクを知っている全員」にできない
  本番シート向けに、`GOOGLE_SERVICE_ACCOUNT_JSON`（サービスアカウントのJSONキー丸ごと）が
  設定されていれば、`_fetch_via_service_account`（`gspread`経由）を優先して使う。
  未設定なら従来の匿名CSV方式にフォールバックする。シート側で、サービスアカウントの
  メールアドレス（`〜@プロジェクト名.iam.gserviceaccount.com`）を閲覧者として共有しておく必要がある。
- **列のマッピング**：スプシの**ヘッダ名がそのまま** `{項目名}` に対応（列『電話番号』→ `{電話番号}`）。
- **絞り込み**：`trigger_col`（既定『ステータス』）が `trigger_val`（既定『未エントリー』）の行のみ。
- **二重申請の防止**：読み取り専用で**スプシへ書き戻せない**ため、処理済み行のキー
  （`_row_key`、ステータス列は除外）を **Supabase の `config_json._processed_keys`** に保存し、
  再実行時はスキップする。直近 `PROCESSED_KEYS_LIMIT`（=20000）件に制限。
- **重複ヘッダ検出**（`_parse_pending`）：同名の見出しがあると `csv.DictReader` が後勝ちで
  値を取りこぼすため、重複を検出したら**明示エラー**にして誤申請を防ぐ。
- **本番ゲート**（`ENKAN_ALLOW_LIVE`）：既定は**ドライラン**（対象を表示するだけ／実ブラウザ操作なし）。
  `1` のときのみ実申請。ワークフローでは手動実行 `live=true` のときだけ ON、
  **スケジュール実行は常にドライラン**（事故防止）。＝**無人スケジュールでは申請されない**。
  実申請は人が `live=true` を押したときのみ。
- ⚠️ **書き戻し不可の制約**：read-only のためスプシの「ステータス」列は自動更新されない
  （担当者の目視では未処理のまま見える）。二重申請は `_processed_keys` で防ぐ。
  ステータス書き戻しが必要ならサービスアカウント方式 or Apps Script 方式への切替が前提（ロードマップ）。

### 🛡️ 申請の信頼性（`run_robot` / `run_all_active` の安全装置）

- **申請完了の確認**（偽成功の防止）：`robot_config.success_text`（任意・司令室で設定）を入れると、
  送信ステップ実行後に**完了画面の文言／URL**を確認する。確認できなければ失敗扱いにし
  `_processed_keys` に入れない＝**再申請可能**。未設定時は送信するが「成功は自動確認できていない」旨を警告。
- **送信ボタンの確実化**：送信（申請）ステップのフォールバックでは
  `input[type=submit]`/`button[type=submit]` を常に候補に含め、「申請する」等でも押せるようにする。
- **dedup キーの安定化**（`_row_key`）：値を `NFKC`＋空白正規化してからハッシュ。無関係な表記揺れ
  （全角半角・末尾空白）での誤再申請を抑止。既存キーとは `_row_key_legacy` を併用して後方互換。
  `spreadsheet.dedup_cols`（任意）を指定すると、その安定列だけでキーを作る。
- **処理済みキーの保存**（`_persist_processed_keys`）：成功 **1 件ごと**に保存（途中クラッシュでの巻き戻り防止）。
  保存は**最新の `config_json` を読み直して `_processed_keys` だけ更新**（司令室編集の踏み潰し＝lost update 防止）。
  追記順を保持（`dict.fromkeys`）し上限超過時は古い順に切り捨て＋警告。
- **stealth**（`robot_config.stealth`）：ON で `slow_mo` を効かせる（headless でもゆっくり操作）。
  従来は設定が無視されていた不具合を修正。
- **CAPTCHA**：自動突破は**未対応**。検出（`_looks_blocked`）したら送信せず安全停止（UI 文言も実態に修正済み）。

### 🔔 通知と実行サマリ（無人運用の観測性）

- **Slack 通知**（`notify_slack`・opt-in）：`SLACK_WEBHOOK_URL`（env or secrets）がある時のみ送信。
  各行の完了（`slack_msg` の `{項目名}` を置換）／失敗・中止／ロボット単位サマリを通知。未設定なら何もしない。
  `notifications.slack_id` は Incoming Webhook では宛先指定に使えないため**本文の目印**として前置するだけ。
- **実行サマリ**（`_write_run_summary`）：1 回の実行結果（台数・成否・モード）を
  `artifacts/run_summary_*.json` に保存（ワークフローが成果物としてアップロード）。

## 🛣️ ロードマップ（README より）

- [x] GitHub Actions による毎日自動実行（クラウド稼働）
- [x] SFA スプシからの自動読み込み（リンク共有・読み取り専用、`fetch_pending_rows`）
- [ ] ステータス書き戻し（read-only のため未対応。要・サービスアカウント方式）
- [ ] `exec()` の構造化アクション置き換え（セキュリティ向上）
- [ ] 進捗ダッシュボード／進捗反映（取り込み〜反映の実行）／変更キャンセル管理＋Slack 通知／設定モード

## ⚠️ 既知の注意点

- `robot.py` は `ai_code` を `exec()` で実行している。AI 生成コードを動的実行するため、
  信頼できる入力前提。将来的に構造化アクションへ置き換える方針（ロードマップ参照）。
- 手順書の編集で空セルが `NaN`/`None` になる問題に対し、保存時に空文字へ正規化する処理がある。
- ブラウザは、ローカル（有人）では表示・10秒待機で目視でき、クラウド（CI）では headless で動く。
